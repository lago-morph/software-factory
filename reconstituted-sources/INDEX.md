# `reconstituted-sources/` — INDEX

**Status: REGENERATING.** A bug was found in the source-recovery script (an `awk` `index()` returning 0 caused spurious matches on shorter paths). This corrupted ~105 files' content with wrong large blobs. Recovery has been re-run with the bug fixed and the file content is now correct. INDEX.md summaries for the corrected files are being regenerated by 14 parallel subagents — this stub will be replaced when they complete.

The prior (partially-wrong) INDEX content is preserved as `INDEX-WIP-STALE.md` for diff purposes; do not rely on it for the 105 affected files.

See `PLAN.md` for the full recovery plan.
