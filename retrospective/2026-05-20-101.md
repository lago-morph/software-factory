# Retrospective — Gas City + Gas Town substrate analysis and downstream Q&A

- **UTC date**: 2026-05-20 (verified via `date -u +%Y-%m-%d` and `python3 datetime.now(datetime.UTC)` — both returned `2026-05-20`)
- **Last PR**: #101 (merged 2026-05-20 as `a6d6eff`)
- **Branch at write time**: `claude/analyze-gas-systems-UccYK`
- **Sibling artifacts**: [./2026-05-20-101/](./2026-05-20-101/)
- **Scope**: full session (from branch divergence — no prior retrospective committed in this session)

## Commit hashes by PR

### PR #101 — `claude/analyze-gas-systems-UccYK` (merged 2026-05-20)

- `983b4d8` research: gas-systems substrate analysis (report 38 + followups 13/14)

The session continued past PR-merge with two follow-up Q&A turns and the retrospective itself — those are not under a PR.

## Part 1 — what happened

### Phase 1 — Scoping and brief preparation

The session opened with the user requesting a Gas City + Gas Town architecture and capabilities assessment, with explicit framing as candidate execution substrate for the StrongDM Dark Factory and Every.to Compound Engineering methodologies. The user's instructions were unusually specific: clone both repos to `/tmp`, spawn subagents to analyze them, write a main report in `research/`, put detailed deep-dives in `research/followup/` for a future AI session, default to the more thorough option without asking. The user also gave the GitHub URLs (`gastownhall/gascity` and `gastownhall/gastown`) and the docs site (`docs.gascityhall.com`).

The foreground work before subagent dispatch was substantial: AGENTS.md (for PR-draft policy: ready-for-review by default, not draft); the strongdm + dark factory + compound engineering reports (full); the prior gastown-beads comparison at `research/followup/04`; the architectures comparison; and ground-truth peeks at gascity/README, gastown/README, `coming-from-gastown.md`, `nine-concepts.md`, `gastown/docs/design/architecture.md`. The docs site at `docs.gascityhall.com` returned HTTP 403 from the sandbox — noted and worked around by using the in-repo Mintlify source. By the time the subagent briefs were authored, both repos had been mapped to a high-level shape (Gas City as substrate, Gas Town as canonical application) and the comparative axis ("primitive substrate vs canonical application") had been chosen as the load-bearing analytical lens.

### Phase 2 — Parallel subagent dispatch

Two general-purpose subagents were dispatched in parallel with thorough briefs — 18 explicit scope items for the Gas City subagent, 21 for the Gas Town subagent — and the instruction to write deep architecture references at the 4–8k-word range with citation discipline (every claim must cite a file path). Both ran in the background. While they ran (~12 minutes each), the foreground continued ground-truth reads of `02-strongdm-attractor.md`, `architectures/00-comparison.md`, and the gas city `nine-concepts.md` doc; the goal was to enter the synthesis phase with a verified mental model of both substrates' primitive sets so subagent output could be spot-checked rather than trusted blindly.

Both subagents returned within the wait window. The Gas City analysis ran ~10,200 words across 18 sections with 55+ internal-package citations; the Gas Town analysis ran ~9,300 words across 21 sections with ~250 cobra-command coverage and ~30 design-doc citations. Both arrived in `/tmp/gascity-analysis.md` and `/tmp/gastown-analysis.md`. A targeted re-read of each spot-checked the load-bearing claims — Nine Concepts mapping, ZFC + Bitter Lesson invariants, the gastown role taxonomy as a pack rather than as Go code, the `gt-proxy-server` mTLS sandboxing model — against the ground-truth reads from Phase 1.

### Phase 3 — Synthesis writing

The synthesis report at `research/38-gas-systems-substrate.md` was authored next, ~7,900 words, with two structural pivots: (a) the substrate-vs-application split as the central architectural fact, framed via verbatim quotes from `gascity/AGENTS.md:1-18`; (b) two concrete deployment sketches — one for Dark Factory on Gas City, one for Compound Engineering on Gas City + the Gastown pack — each with `city.toml` excerpts, formula TOML, and order TOML examples. The mapping tables in §2 and §3 are the load-bearing artifact: every Dark Factory primitive and every Compound Engineering primitive gets a row pointing at the Gas City realisation (substrate-level, pack-level, or methodology-supplied convention) with an explicit fit grade.

The two subagent outputs were placed at `research/followup/13-gas-city-deep-dive.md` and `research/followup/14-gas-town-deep-dive.md` with prefatory headers (audience statement, provenance block, companion-document cross-links). `research/INDEX.md` was updated with row 38, rows 13 and 14 in the followup section, and an extended "Last updated" stamp acknowledging the 2026-05-20 round.

### Phase 4 — PR, monitoring, and merge

The diff was committed (one commit, 3,418 insertions across 4 files), pushed to `claude/analyze-gas-systems-UccYK`, and opened as PR #101 — ready-for-review per `AGENTS.md`'s explicit override of the harness default. The PR description carried five numbered reviewer test-plan checkboxes and a "notes for reviewers" provenance block (blocked-URL caveat, subagent-authorship caveat, supersedes-prior-report note for followup/04). The user subscribed to PR activity via the webhook system reminder; no CI ran (research-only diff, no workflows configured); no review comments arrived. The PR merged shortly after as commit `a6d6eff` on main. The webhook unsubscribed automatically with a "PR merged" terminal-state notification.

### Phase 5 — Post-merge Q&A

After merge, the user asked two follow-up questions. **Q1**: did the deep Gas City analysis cover pack composability with examples? Answered by `grep -nE "pack|Pack" research/followup/13-gas-city-deep-dive.md` plus targeted Reads of §4 (Pack Composition with worked `pack.toml`) and §14 (Pack Ecosystem with 5 bundled packs + import model). The honest answer was: yes, the deep-dive covers the *mechanics* (syntax, composition pipeline, override layering); the *use-case scenarios* live in the synthesis report's §4 and §5 deployment sketches. Offered to add use-case examples to deep-dive §14 as a small targeted edit.

**Q2**: assess feasibility of running this repo's processes (the research-synthesis pipeline + cleanup work) as a Gas City rig using the Gas Town pack, particularly around cleaning up things and doing the rest of the syntheses. Read `research/PLAN.md` in full (538 lines, ~52k tokens) and `research-plan.md` (35 lines). Produced a structured feasibility assessment: verdict ("highly feasible, but cost/benefit pivots on which subset"); backlog decomposed into 5 buckets (cross-corpus sweeps; §3.2 curated edits; F36/F37 numbering collision; synthesis collapse + v3 architecture; retros + housekeeping; deferred stage-5 drains); primitive-mapping table; what's awkward (Refinery overkill at 1-3 PRs/day, Wasteland N/A single-org, unified-synthesis bucket genuinely sequential); concrete `city.toml` + formula example; cost-benefit; middle-path recommendation (adopt Beads only, defer the rest of the substrate). The user did not ask for the assessment to be committed; it ran as chat output.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 2 (parallel deep-architecture analysis, ~12 min each, run in background) |
| PRs opened / merged | 1 / 1 (PR #101) |
| Real-world bugs discovered + fixed | 0 (research/docs contribution; no code change) |
| Tests added (before / after) | n/a |
| Spec amendments | 0 |
| Scenarios driven / skipped | n/a |
| Files touched at major refactors | 4 (research/38 + followup/13 + followup/14 + research/INDEX.md) |
| Words written (committed) | ~27,500 across the three new files |
| Words read (foreground, this session) | ~45,000 (the four prior reports + both PLAN files + both deep-dive outputs + ground-truth docs) |
| Q&A turns post-merge | 2 (pack-composability follow-up + repo-as-rig feasibility) |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `substrate-fit-assessment` | SKILL-SPEC-5782bfb04d | medium | feasibility-assessment shape | [./2026-05-20-101/SKILL-SPEC-5782bfb04d-substrate-fit-assessment.md](./2026-05-20-101/SKILL-SPEC-5782bfb04d-substrate-fit-assessment.md) |

One skill candidate. This session produced three instances of the substrate-fit shape (Dark Factory on Gas City, Compound Engineering on Gas City, and the repo's own pipeline on Gas City + Gastown pack) — three is enough evidence to extract a reusable template. The skill is specifically about the structured-feasibility-assessment shape, not about parallel subagent dispatch (already covered by `parallel-subagent-fanout`) or analysis briefing (`subagent-prompting`). Priority is medium because the trigger is recurring — the parent project is sizing up candidate execution substrates and will likely produce more of these — but not high-frequency.

## Part 3 — agents-file suggestions

Proposed additions to the project's agents file (`AGENTS.md`). Each rule has its own per-rule file in the sibling directory at `./2026-05-20-101/AGENTS-MD-<hash>-<kebab-name>.md` — the per-rule file contains exactly two sections: `# agent instruction` (the verbatim rule text the CI/CD assembler concatenates into `AGENTS.md`) and `# justification` (the persuasion text the assembler strips out).

This Part 3 mirrors that content inline so the retrospective reviewer has every proposed rule + its justification in one place at review-decision time.

### Suggestion 1: Pair synthesis reports with companion deep-dive followups — `AGENTS-MD-6d2ec706a7`

- **Per-rule file**: [./2026-05-20-101/AGENTS-MD-6d2ec706a7-pair-synthesis-with-followup-deep-dives.md](./2026-05-20-101/AGENTS-MD-6d2ec706a7-pair-synthesis-with-followup-deep-dives.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Pair synthesis reports with companion deep-dive followups.** When a session produces a dense analysis of a new corpus source (especially via parallel subagent dispatch), commit both a synthesis report at the top level (`research/NN-<slug>.md`) AND companion deep-dive references in `research/followup/MM-<slug>.md`. The synthesis carries the actionable mapping and cross-source ties; the followup carries the verbatim subagent output as the durable reference. Both must be registered in `research/INDEX.md` in the same commit.
>
> *Grounded in: PR #101 — report 38 + followup/13 + followup/14 (Gas City / Gas Town substrate analysis).*

**Justification** (verbatim from the per-rule file's `# justification` section):

PR #101 dispatched two parallel subagents that produced ~10.2k + ~9.3k words of citation-rich architecture references for Gas City and Gas Town. The synthesis report (research/38, ~7.9k words) mapped both onto the Dark Factory and Compound Engineering primitive sets — that is the actionable artifact a reviewer of the parent project's architecture comparison can use. The two deep-dives are the durable reference future AI sessions will load when they need to know which `internal/` package implements the bead store or how `gt seance` routes predecessor-session queries.

Committing only the synthesis would have orphaned ~20k words of careful subagent output that the synthesis itself depends on; committing only the deep-dives would have left no usable mapping into the parent project's failure-mode catalogue. The corpus already enforces this pattern unevenly — report 03 (Every compound engineering) → followup/05 + followup/11; report 07 (Dark Factory) → followup/04 — but the pattern is implicit, so future sessions sometimes ship only one half. Making the pairing an explicit rule turns "the pattern most corpus contributions accidentally followed" into "the pattern every dense-source contribution explicitly follows," and the INDEX.md co-registration requirement makes the pair findable from a single grep.

The marginal cost is one extra file per source plus two extra INDEX rows. The cost of skipping it is the next session has to either re-do the deep dive (token waste) or ship a synthesis whose claims they cannot verify (provenance erosion).

### Suggestion 2: Read load-bearing source docs while subagents run — `AGENTS-MD-3b41292ea7`

- **Per-rule file**: [./2026-05-20-101/AGENTS-MD-3b41292ea7-read-source-docs-while-subagents-run.md](./2026-05-20-101/AGENTS-MD-3b41292ea7-read-source-docs-while-subagents-run.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Read load-bearing source docs while subagents run.** When dispatching deep-analysis subagents against a large repo or corpus, use the foreground wait to read 3–5 load-bearing docs from the target (typically the overview / architecture / coming-from-X files) so subagent output can be spot-checked before it commits to the corpus. The marginal cost is ~10 minutes of foreground reads; the benefit is catching paraphrased or confabulated claims before they propagate into a permanent reference.
>
> *Grounded in: PR #101 — parallel ground-truth reads of nine-concepts.md and gastown/docs/design/architecture.md while subagents ran.*

**Justification** (verbatim from the per-rule file's `# justification` section):

PR #101's two subagents ran for ~12 minutes each in the background. During the wait, the foreground session read `gascity/docs/getting-started/coming-from-gastown.md`, `gascity/engdocs/architecture/nine-concepts.md`, and `gastown/docs/design/architecture.md` end-to-end. This produced a high-confidence ground-truth grasp of the "Nine Concepts" primitive set, the substrate-vs-application split, and the two-level Beads/Dolt routing — which is exactly what the subagent outputs were going to claim. When both analyses returned, the load-bearing claims (Nine Concepts mapping, ZFC + Bitter Lesson invariants, gastown role taxonomy as a pack) were verifiable against an already-loaded mental model, not against scrollback or re-reads.

The previous corpus has at least one known instance of subagent claim drift — `research/followup/04-gastown-beads.md` characterised `gt-proxy-server` as "for routing LLM API calls (model routing / quota management)" based on a partial read; the new followup/14 walk against the actual `internal/proxy/` code reveals it is **containerized polecat isolation via mTLS**, not LLM routing. That refutation is now in followup/14 and the synthesis report. The foreground reads on this session caught nothing that bad (subagent output was clean), but the discipline scales the audit trail — every load-bearing claim in the deep-dives could be cross-checked in real time, not in a separate post-commit pass.

Cost: ~10 minutes of foreground reads during a wait that would otherwise be idle. Benefit: subagent output ships with verified load-bearing claims, not "probably correct, will audit later" claims. The corpus has accumulated enough "verbatim quotes that turned out to be confabulated" entries (every revision-notes section of report 01, 02, 07 catalogues several) that the marginal cost of foreground verification pays for itself within one session.

### Suggestion 3: Include at least one use-case scenario per major-primitive section in deep architecture references — `AGENTS-MD-945a36b56a`

- **Per-rule file**: [./2026-05-20-101/AGENTS-MD-945a36b56a-use-case-per-primitive-in-deep-references.md](./2026-05-20-101/AGENTS-MD-945a36b56a-use-case-per-primitive-in-deep-references.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Include at least one use-case scenario per major-primitive section in deep architecture references.** When writing a deep architecture reference intended for a future AI session (e.g., `research/followup/`), each major-primitive section should include at least one motivating use-case scenario — not just the syntactic mechanics of the primitive. A reader of the document in isolation must be able to answer "what would I actually compose / configure this for?" without chasing cross-links to a separate synthesis report.
>
> *Grounded in: PR #101 follow-up Q1 — gas city deep-dive covered pack-composition mechanics in §4 + §14 but use-case scenarios lived only in the synthesis report §4 + §5.*

**Justification** (verbatim from the per-rule file's `# justification` section):

After PR #101 merged, the user asked: *"Did the detailed analysis of gas city talk about the fact that it is broken down into packs that are composable and give some examples of how that could be used?"* The honest answer was: yes, the deep-dive covered the mechanics of pack composability comprehensively (§4 has the `pack.toml` `[imports.gastown]` + `[imports.review]` example, the six-step composition pipeline, the override-cascade table; §14 enumerates the 5 bundled packs and the `gc import add/install/upgrade/why` model). But the *worked use-case scenarios* — "compose a darkfactory pack alongside gastown for scenario-driven build," "compose a compound pack on top of gastown with 14 reviewer agents" — lived only in the synthesis report.

That asymmetry is structurally inappropriate for a deep-dive whose audience is "a future AI session that needs to understand this tool well enough to use it, extend it, or treat it as a runtime substrate." A reader who loads only the deep-dive (the natural reading mode for a deep reference) sees the syntax of `[imports.X]` but cannot answer "what would I actually compose this for?" without loading a separate file. The fix is one to two paragraphs per major primitive section — e.g., §14 Pack Ecosystem should include a "compose a security-review pack into an existing town" or "swap the gastown.polecat provider per-rig" scenario alongside the bundled-packs table.

Marginal cost: ~5 minutes per primitive section. Cost of skipping: every reader of the deep-dive in isolation has to either chase the synthesis-report cross-link (slow) or reason from syntax to use-case themselves (error-prone). The pattern generalises to any architecture reference written for asynchronous-reader audiences (which is most of `research/followup/`).

### Suggestion 4: PR descriptions for research contributions include a numbered reviewer test plan — `AGENTS-MD-fb2a74414f`

- **Per-rule file**: [./2026-05-20-101/AGENTS-MD-fb2a74414f-pr-test-plan-checklist.md](./2026-05-20-101/AGENTS-MD-fb2a74414f-pr-test-plan-checklist.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **PR descriptions for research contributions include a numbered reviewer test plan.** Pull request descriptions for research-corpus contributions (reports, followups, synthesis, INDEX updates) must include a numbered reviewer test plan that names specific files and specific claims to spot-check, plus a "notes for reviewers" section calling out provenance caveats (blocked URLs, subagent authorship, supersedes-prior-report notes). Generic "verify the new files look right" descriptions defer the audit-burden onto the reviewer; explicit checklists make the audit cheap.
>
> *Grounded in: PR #101 — reviewer test-plan with 5 specific spot-check items + "notes for reviewers" provenance block earned a fast merge.*

**Justification** (verbatim from the per-rule file's `# justification` section):

PR #101 included an explicit reviewer test plan with five specific checkboxes — verify the three new files are at expected paths and INDEX rows are present; spot-check three architectural claims in followup/13 against cited `gascity/...` paths; spot-check three claims in followup/14 against `gastown/...` paths; sanity-check the two mapping tables against the source reports; confirm the deployment-sketch sections cite Gas City features that actually exist at v1.0.0+. The "notes for reviewers" section also surfaced the blocked-URL caveat (docs.gascityhall.com → HTTP 403 from sandbox; analysis worked from in-repo Mintlify source), the subagent-authorship caveat (each deep-dive ran ~12 minutes with full read-only filesystem access; outputs spot-checked against foreground reads before commit), and the supersedes-prior-report note (followup/14 supersedes followup/04 on internal-package structure while remaining consistent on the Attractor-comparison axis).

The user merged the PR fast. The audit cost was concretely scoped — five tests, named files, named claims — and the provenance caveats meant no reviewer-side investigation was needed to understand why certain claims looked the way they did. Compare to a generic "Adds Gas City + Gas Town analysis" description: the reviewer would have to either trust the diff or open a 27k-word read to verify. Either outcome is worse.

The marginal cost is ~5 minutes of PR-description authorship per dense contribution. The benefit is the audit becomes a 10-minute reviewer pass instead of a 30-minute reviewer pass, and the provenance caveats are visible at decision time rather than discoverable only by re-reading the diff. Generalises to any research-corpus PR whose diff is denser than a reviewer can plausibly inspect line-by-line.

### Suggestion 5: Grep before recall when asked whether prior session work covered a topic — `AGENTS-MD-b30271a2a5`

- **Per-rule file**: [./2026-05-20-101/AGENTS-MD-b30271a2a5-grep-before-recall.md](./2026-05-20-101/AGENTS-MD-b30271a2a5-grep-before-recall.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Grep before recall when asked whether prior session work covered a topic.** When the user asks "did I do X?" or "does the prior analysis cover Y?", answer by running `grep` / `Read` against the actual file, not by reconstructing from session memory. Session memory after 50k+ tokens is unreliable; grep is one tool call. If the answer is "yes but in a different section than expected," point to the actual section and cite the lines.
>
> *Grounded in: PR #101 follow-up Q1 — answered "did the gas city deep-dive include pack composability examples?" by grep + targeted Read, not by memory.*

**Justification** (verbatim from the per-rule file's `# justification` section):

After PR #101 merged, the user asked whether the Gas City deep-dive covered pack composability with examples. The session had been running ~60k tokens deep, with two ~10k-word subagent outputs in scrollback. Reconstructing "did the analysis cover X" from memory at that depth is unreliable — the boundary between "I saw a section like that" and "I added that to the synthesis report instead" blurs.

The cheap and correct move was: `grep -nE "pack|Pack" research/followup/13-gas-city-deep-dive.md | head -40`, followed by a targeted `Read` of the two sections most likely to be relevant (§4 Pack Composition, §14 Pack Ecosystem). That produced a precise, citation-rich answer in ~2 tool calls: the deep-dive covers the mechanics in §4 and §14; the use-case scenarios live in the synthesis report §4 and §5; here are the line numbers. The user got verifiable structure instead of "yes I think we covered it."

The marginal cost is one grep + one targeted Read — under a minute. The cost of recall-from-memory is either confidently-wrong assertions (which the user can catch but then loses trust) or hedged "I think we covered it" responses (which force the user to grep anyway). The discipline generalises far beyond the retrospective context — any time the user asks a question whose ground truth is on disk, the right answer pulls from disk.

## Part 4 — proposed ADRs

Architectural decisions made in this session that the user may want to record as ADRs. Each candidate has a **full draft** in the sibling directory, carrying a frozen `ADR-<hash>` ID. The user can adopt any draft by invoking the `adr` skill, which moves it to `docs/adr/NNNN-kebab-title.md` while preserving the ID.

| ID | Title | Draft |
|----|-------|-------|
| ADR-cf8bdf804a | Use the synthesis-plus-companion-deep-dive pattern for dense corpus contributions | [./2026-05-20-101/ADR-cf8bdf804a-synthesis-plus-companion-deep-dive-pairing.md](./2026-05-20-101/ADR-cf8bdf804a-synthesis-plus-companion-deep-dive-pairing.md) |

One ADR draft. This codifies what Suggestion 1 operationalises — the corpus already follows the pairing pattern unevenly; recording it as a binding decision turns implicit-pattern into explicit-contract for future research authorship. The session also surfaced a second candidate ("migrate `research/PLAN.md`'s tabular state to Beads") during the post-merge feasibility-assessment phase, but that is a *recommendation to the user* about the wider architectural direction, not a *decision I made in this session*. It belongs in the user's decision queue, not as a proposed ADR from this retrospective.
