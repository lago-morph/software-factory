# Retrospective — issue #105 source-ingestion + issue-management skill hardening

- **UTC date**: 2026-05-21 (verified via `date -u +%Y-%m-%d` and `python3 -c "import datetime; print(datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%d'))"`, agreed)
- **Last PR**: #117 (highest PR number covered by this retro)
- **Branch at write time**: `claude/issue-management-loaded-on-any-access`
- **Scope**: full session (from branch divergence) — no prior retrospective committed during this session
- **Sibling artifacts**: [./2026-05-21-117/](./2026-05-21-117/)

## Commit hashes by PR

### PR #116 — `claude/fix-issue-105-BY8ZJ` (merged 2026-05-21 03:31 -0500)

- `5138ac8` Ingest spec-driven-ai-dev.md as cataloged source (issue #105)

### PR #117 — `claude/issue-management-loaded-on-any-access` (merged 2026-05-21 03:33 -0500)

- `b5ca9f2` issue-management: require skill load on any issue access
- `fb9e614` issue-management: self-installing PreToolUse gate + CREATE-ISSUE behavior

### Issues created (no PR)

- Issue #118 — "Generalize the issue-management PreToolUse gate pattern to other process skills" (filed via the new CREATE-ISSUE behavior; carries `help wanted`).

## Part 1 — what happened

### Phase 1 — Fix issue #105 (source ingestion)

Goal: ingest the root-level `spec-driven-ai-dev.md` into the source catalog, reframe references across `/research`, do "the most minimal edits" per the issue body. I loaded `research-pipeline`, ran its pre-flight, computed `id = 3592091691` from the user-supplied URL (`https://github.com/jonathanmanton`), moved the file to `reference-only/3592091691/`, registered the record, regenerated `sources.md`, retired the now-obsolete planning tasks in `research/PLAN.md` and `research-plan.md`, added short status notes to the report sections that proposed amendments (Reports 8, 14, 24, 25, 26, followup/12, harness run report), updated ADR-0002's open-question parenthetical, added the issue URL to `MIGRATION-EXCEPTIONS.md`, committed, pushed, opened PR #116. Lint passed (`bash scripts/lint-sources.sh` → `✓ all checks passed`).

What I missed: `issue-management` was never loaded. Every `mcp__github__issue_read` and `mcp__github__add_issue_comment` call happened ungoverned. The PR was opened with `Closes #105` but no STARTED claim was on the issue, no `good first issue` label, no PR-OPENED comment. The user caught it in the next turn.

### Phase 2 — Backfill the missed `issue-management` conventions

The user asked "What branch are you on? And why aren't you using the issue management skill?" One sentence, decisive. I loaded the skill, ran `get_me`, applied the assignee and the `good first issue` label, posted a `[STARTED]` comment with an explicit "Posted retroactively" preamble, and a `[PR-OPENED]` comment pointing at PR #116. The thread now reads as a clean lifecycle even though the events were posted out of order. The webhook subscription on PR #116 was active; CI was pending.

### Phase 3 — Tighten the skill description (R0)

User: "Please modify the description at the top of the issue management skill to say that you must load skills when accessing issues in any way." I edited the YAML `description` and the body's "When to use primary mode" section to call out the gated MCP tool names by exact identifier (`mcp__github__issue_read` etc.), kill the "I'm only reading" / "I'll load it later" carve-outs, and document the `<github-webhook-activity>` event surface. Stacked the change on a fresh branch (`claude/issue-management-loaded-on-any-access`) off `main` rather than bundling into PR #116, opened PR #117.

### Phase 4 — Recommend mechanisms against future skips (R1–R6)

User: "I do not want it getting skipped because you are distracted… Recommend changes across any of my skills that will prevent this in the future. You must not implement the changes until I review your recommendations." I drafted six recommendations ranked roughly from most-robust (PreToolUse hook that gates issue tools on skill load) to least-robust (AGENTS.md hard rule). The point I led with: the description tightening alone is still attentional and won't help — the failure mode needs deterministic enforcement.

### Phase 5 — Implement R1 + new CREATE-ISSUE behavior

User: approved R1 with two caveats — the skill self-installs and checks on every load that the hook is properly installed (replicating the `architecture-failure-mode-gate` pattern); add a CREATE-ISSUE behavior that silently applies the `help wanted` label whenever the agent files a new issue (the label is a permanent marker of agent authorship); then create an issue (using the new behavior) to track R2 (generalize the gate to other process skills); defer R5 to this retrospective so it lands via the standard `AGENTS-MD-<hash>` path.

I wrote:
- `.claude/skills/issue-management/resources/hooks/require-issue-management-skill.sh` — the hook script template (marker-based allow/block, with an instructive stderr on block).
- `.claude/skills/issue-management/scripts/install.py` — self-syncing installer with `--check` / `--force` / `--dry-run` / `--no-commit` modes; idempotent JSON-merge into `.claude/settings.json`; touches `/tmp/.claude-skill-loaded-issue-management` on success.
- SKILL.md — added the "STOP — run the pre-flight" stanza, the CREATE-ISSUE behavior section (silent, intentionally no comment), and updated the quick-ref table, tags-in-use list, label-lifecycle summary, workflow blocks, and Bundled-files section.

Ran the installer end-to-end (`--check` correctly reported drift, `--force` installed both artifacts, the hook blocked when the marker was absent and allowed when present), filed issue #118 via the new CREATE-ISSUE behavior (the `help wanted` label landed in the single create call), committed, pushed.

### Phase 6 — Merge wave

Both PRs merged cleanly. PR #116 auto-closed issue #105 via `Closes #105` with `state_reason=completed`; the `good first issue` label was preserved per the skill convention ("kept — work landed"). PR #117 landed the gate on `main`, so every subsequent session in a fresh sandbox inherits the gate from the clone — the known limitation ("first session in a fresh repo can still miss the skill load") only applies to sessions that haven't pulled the post-merge `main`. Webhook subscriptions on both PRs cleared.

### Metrics

| Metric | Value |
|--------|-------|
| PRs opened / merged | 2 / 2 (#116, #117) |
| Issues created via new behavior | 1 (#118) |
| Subagents dispatched | 0 (none needed; all work was small-fan-out edits) |
| Real-world bugs discovered + fixed | 1 (missed skill load on #105, backfilled) |
| Skills modified | 1 (`issue-management`) |
| New scripts created | 1 (`install.py`) |
| New hook scripts installed | 1 (`require-issue-management-skill.sh`) |
| New settings.json entries | 1 (PreToolUse hook for issue tools) |
| Files touched at PR #116 | 14 |
| Files touched at PR #117 | 5 |
| Lint runs | 3 (`bash scripts/lint-sources.sh` — all `✓ all checks passed`) |
| Recommendations drafted | 6 (R1–R6); 1 implemented this session (R1), 1 filed as issue (R2), 1 deferred to this retro (R5) |

## Part 2 — skills summary

No new skill candidates surfaced. The session extended one existing skill (`issue-management`) with a pattern (self-installing gate) that already exists in another existing skill (`architecture-failure-mode-gate`'s install.py + workflow-template structure). Issue #118 captures the R2 generalization (extracting the shared bits to `.claude/lib/`) as a tracked follow-up rather than a new skill spec — the right artifact for "lift the helper" work is an issue, not a skill spec.

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| *(none)* | — | — | — | — |

## Part 3 — agents-file suggestions

Three rules earned a place. The first one is the R5 deferral the user explicitly requested.

### Suggestion 1: Process skills — non-negotiable triggers — `AGENTS-MD-9573ff5b60`

- **Per-rule file**: [./2026-05-21-117/AGENTS-MD-9573ff5b60-process-skills-non-negotiable-triggers.md](./2026-05-21-117/AGENTS-MD-9573ff5b60-process-skills-non-negotiable-triggers.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Process skills — non-negotiable triggers.** Certain skills govern conventions that must fire on every interaction with a class of tool surface, regardless of the user's stated task. Load them the first time their gated tool surface comes up in a session, not after. The current process skills are: `issue-management` (gates `mcp__github__issue_*`, `add_issue_comment`, `list_issues`, `search_issues`, `sub_issue_write`, and any reference to an issue number in a commit, PR, or plan doc); `always-commit-skill-to-repo` (gates `git commit`, `git push`, and the PR-write MCP tools); `in-flight-workflow-tracking` (gates long-running dispatch — subagent fanout, PR-activity subscription, fetch-blocked-urls issue creation). Carve-outs like "I'm only reading" or "I'll load it when I actually do something" are not valid. When a prompt triggers more than one skill (e.g. "fix issue 105 — ingest a source" hits both `research-pipeline` and `issue-management`), load all of them, not the most salient one.
>
> *Grounded in: PR #116, where "fix issue 105" loaded `research-pipeline` for the source-ingestion content but skipped `issue-management` until the user pointed it out, leaving the STARTED claim and PR-OPENED comment unposted on the issue.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The PR #116 session opened with "Fix issue 105." The issue body was about source ingestion, so I loaded `research-pipeline` and went straight to catalog work. Every `mcp__github__issue_read` and `mcp__github__add_issue_comment` call after that happened without `issue-management` ever being loaded — the STARTED claim, the `good first issue` label, the per-event comments. The user caught this one turn before the PR opened. Backfilling cost ~5 minutes of tool calls and a "Posted retroactively" preamble on every comment. The next slip in a less-attentive review window costs the audit trail entirely.

The cause is what the user named in chat: salience collapse. When a prompt names a vivid work-content trigger (source ingestion, fetch, drain, fanout), the process skill that governs the meta-activity (issue interaction, git ops, long-running dispatch) gets drowned out. The fix is mechanical at the gate level (PR #117 ships the `issue-management` PreToolUse gate that hard-blocks this exact mistake), but the doc rule belongs in `AGENTS.md` so it lands in front of the agent at session start regardless of which skills get auto-loaded. The marginal cost of the rule is zero — readers already scan AGENTS.md. The cost of skipping it is the slip we just experienced, recurring quietly in every session that hasn't yet picked up the gate.

### Suggestion 2: Self-installing pattern for cross-tree artifacts — `AGENTS-MD-0df77e3717`

- **Per-rule file**: [./2026-05-21-117/AGENTS-MD-0df77e3717-self-installing-pattern-for-cross-tree-artifacts.md](./2026-05-21-117/AGENTS-MD-0df77e3717-self-installing-pattern-for-cross-tree-artifacts.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Self-installing pattern for cross-tree artifacts.** Any skill that writes files outside its own directory — hooks, GitHub workflows, `.claude/settings.json` entries, configs, anything the consumer repo needs installed — must ship a `scripts/install.py` with `--check` / `--force` / `--dry-run` modes and a "STOP — run the pre-flight" stanza at the top of its `SKILL.md`. The pre-flight stanza branches on the install script's exit code (0 = clean, 1 = drift / missing → run `--force` with no user prompt, 2 = skill-install defect → surface and stop). Do not rely on user memory to install; do not rely on a one-time setup step; do not author the cross-tree artifact by hand-edit. Reference implementations: `architecture-failure-mode-gate` (workflows), `issue-management` (hook + settings.json), `research-pipeline` (workflows).
>
> *Grounded in: PR #117, where the issue-management PreToolUse gate became a self-installing skill — install.py + a pre-flight that runs every load and auto-repairs drift — to avoid the "skill says you must install the hook" / "user forgets to install the hook" loop.*

**Justification** (verbatim from the per-rule file's `# justification` section):

PR #117 added a PreToolUse hook that blocks `mcp__github__issue_*` calls unless the `issue-management` skill has been loaded. The hook script + the `.claude/settings.json` entry live outside the skill directory. Without the self-install pattern, this would mean: "Open the skill. Read the instruction. Copy the file to the right place. Edit settings.json. Hope nobody forgot." That's exactly the manual setup step that gets skipped after a few months, and the gate silently stops protecting anything.

The pattern that works — copied from `architecture-failure-mode-gate` and `research-pipeline`'s workflow installers — has three parts: (a) the skill's `SKILL.md` opens with a "STOP — run the pre-flight" stanza naming `install.py --check`, (b) the agent runs `--force` immediately and with no user prompt on a non-zero exit, (c) the installer auto-commits the cross-tree artifacts so they land in the same PR as the skill change. The marginal cost of the pattern is one Python script and ~20 lines of `SKILL.md`. The cost of skipping it is silent drift between what the skill says is true ("there's a gate") and what's installed in the consumer repo ("there isn't"). Issue #118 generalizes this same pattern to `always-commit-skill-to-repo` and `in-flight-workflow-tracking`; the rule preempts future skills from re-discovering the same shape.

### Suggestion 3: Backfill missed conventions on mid-session catch — `AGENTS-MD-6fa84a2732`

- **Per-rule file**: [./2026-05-21-117/AGENTS-MD-6fa84a2732-backfill-missed-conventions-on-mid-session-catch.md](./2026-05-21-117/AGENTS-MD-6fa84a2732-backfill-missed-conventions-on-mid-session-catch.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Backfill missed conventions on mid-session catch.** If the agent discovers it touched a tool surface governed by a process skill without first loading the skill — typically because a gate fired, the user pointed it out, or a downstream check tripped — load the skill immediately and post a retroactively-flagged comment / commit / artifact capturing the events that already happened. Mark the backfill explicitly (e.g. "Posted retroactively — work was already underway and PR #N has just been opened"). Silent skipping erodes the per-issue / per-PR audit trail the conventions exist to maintain.
>
> *Grounded in: PR #116, where the missed `issue-management` load was caught mid-session and the STARTED + PR-OPENED comments were posted on issue #105 with an explicit "Posted retroactively" preamble, restoring the audit trail rather than pretending the slip didn't happen.*

**Justification** (verbatim from the per-rule file's `# justification` section):

When the user pointed out that `issue-management` had been skipped on issue #105, the right move was not to silently move on. The skill defines a STARTED claim, an assignee write, and a `good first issue` label that *should have happened* the moment I first read the issue. Backfilling means posting those things now, with a one-line marker that they're retroactive. The thread then reads as: issue opened → agent claimed it (retro) → PR opened (retro) → PR merged → issue auto-closed. The marker preserves the convention's audit-trail property — a later reader can scan for `[STARTED]` and find one — without rewriting history dishonestly.

The marginal cost of the rule is the few seconds it takes to compose a "Posted retroactively" preamble and a paragraph naming what already happened. The cost of skipping it is two-fold: the audit trail is broken (the conventions exist precisely to be scannable), and the failure becomes invisible to the post-mortem. Misses that get silently elided don't accumulate as feedback; the retrospective two weeks later can't see the slip happened at all. Backfilling makes the miss visible, which is what surfaces it as a candidate for mechanical enforcement (which then became PR #117's PreToolUse gate).

## Part 4 — proposed ADRs

Two binding architectural choices were made in this session. Both are captured as full drafts in the sibling directory so the user can adopt either via the `adr` skill (which moves the file to `docs/adr/NNNN-…` and preserves the `ADR-<hash>` ID).

| ID | Title | Draft |
|----|-------|-------|
| ADR-dec26d09fd | Self-installing PreToolUse hooks as the canonical mechanism for skill-load enforcement | [./2026-05-21-117/ADR-dec26d09fd-self-installing-pretooluse-hooks-for-skill-load-enforcement.md](./2026-05-21-117/ADR-dec26d09fd-self-installing-pretooluse-hooks-for-skill-load-enforcement.md) |
| ADR-1a742b7f91 | Repurpose the GitHub default `help wanted` label as the permanent marker of agent-authored issues | [./2026-05-21-117/ADR-1a742b7f91-help-wanted-label-as-permanent-marker-of-agent-authored-issues.md](./2026-05-21-117/ADR-1a742b7f91-help-wanted-label-as-permanent-marker-of-agent-authored-issues.md) |
