[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --tb=short --strict-markers
markers =
    integration: end-to-end tests that touch the filesystem and run multiple scripts
