---
name: issue-management
description: Conventions for working with GitHub issues in this repository. Triggers whenever the agent is asked to work on, pick up, claim, investigate, answer questions on, or open a pull request for a GitHub issue — and on any phrase like "work on issue #N", "look at issue N", "pick up #N", "address #N", "start on issue", "answer #N". Defines the four required comment events (STARTED, QUESTIONS, ANSWERS, PR-OPENED), the visual template every comment must use, and the GitHub-MCP actions that accompany each event (assignee claim, label, PR-body link to issue). For adding or modifying a behavior, invoke the sibling `add-issue-behavior` skill instead of editing this file by hand.
---

# Skill: issue-management

This skill is a **conventions registry** for working with GitHub issues in
this repository. It does two things:

1. Lists the **behaviors** (events that require the agent to act) and how
   each one is performed.
2. Defines the **comment template** every issue-management comment must
   use, so the issue's comment thread is scannable at a glance: one look
   tells you "agent started", "agent asked", "user answered", "PR opened".

To **add or change** a behavior, do not edit this file directly. Invoke
the sibling skill [`add-issue-behavior`](../add-issue-behavior/SKILL.md);
it walks the user through the information needed and updates this file
plus the templates atomically.

---

## When to use this skill

Read this skill at the start of any turn that involves an issue, including:

- "Work on issue #N" / "pick up #N" / "look at #N" / "address #N".
- "What's the status of issue #N?" — the skill tells you which comments to expect.
- Filing a new issue on the user's behalf — see [behaviour: open-on-behalf](#behavior-open-on-behalf-of-user) (currently not defined; add it via the sub-skill if you need it).
- The user answers a question you previously posted — the **ANSWERS** behavior triggers.
- You're about to open a PR that fixes an issue — the **PR-OPENED** behavior triggers.

Do NOT use this skill for:

- Pull-request review conventions (separate concern; not in scope here).
- Issue-comment text that is purely conversational and not one of the
  defined events. Conversational replies don't use the template.

---

## Repository context

- **Repo**: `lago-morph/software-factory`
- **Authenticated MCP identity**: `jonathanmanton` — the *same* GitHub
  identity as the human user of this repo. There is no separate "bot"
  account. This is why the STARTED comment (with its distinctive header)
  is the load-bearing signal that an *agent* has picked the issue up —
  the commit author and comment author are both `jonathanmanton` either
  way.
- **Label-create not available via MCP** — `get_label` works,
  `create_label` does not exist. Any behavior that wants to apply a label
  must first `get_label` and gracefully fall back if the label is missing.

---

## The behaviors

Each behavior names the **trigger** (when the agent must act), the
**comment type** to post (if any), and the **side effects** on the issue
itself (assignee, label, state). The comment templates live in
`templates/` next to this file.

### Behavior: STARTED (claiming an unclaimed issue)

**Trigger**: The agent is told to work on an issue (e.g. "pick up #N",
"start on #N", "address #N", "work on issue N") **and** the issue has no
current assignee.

**Required action**:

1. `issue_read` method=`get` to confirm the issue is unassigned (the
   `assignees` field is empty) and read its title/body.
2. `issue_write` method=`update` with `assignees: ["jonathanmanton"]` to
   claim it. (If the issue is already assigned to someone else, do NOT
   reassign — instead ask the user whether to proceed.)
3. Apply the `in-progress` label *if it exists* — call `get_label` first.
   If 404, skip the label silently; the STARTED comment is the real
   signal.
4. Post a **STARTED** comment using [`templates/comment-started.md`](./templates/comment-started.md).

**Comment template**: `templates/comment-started.md`.

**Do not** post STARTED if a STARTED comment already exists in the
thread — read `issue_read` method=`get_comments` first to be sure. A
duplicate STARTED is noise.

### Behavior: QUESTIONS (asking the user for clarification)

**Trigger**: The agent has clarifying questions for the user about an
in-flight issue. Each batch of questions gets one comment — do not post
one comment per question.

**Required action**:

1. Group questions logically. Number them so the user's answers can
   reference numbers.
2. Post a **QUESTIONS** comment using [`templates/comment-questions.md`](./templates/comment-questions.md).
3. If the agent's session is about to end before the user can answer,
   record the QUESTIONS comment in the in-flight tracking file per the
   [`in-flight-workflow-tracking`](../in-flight-workflow-tracking/SKILL.md)
   skill.

**Comment template**: `templates/comment-questions.md`.

**One outstanding QUESTIONS comment at a time.** If a previous QUESTIONS
comment hasn't been answered yet, add to that batch rather than opening
a new one (post a new comment that references the prior question
numbers; do not edit the original).

### Behavior: ANSWERS (summarizing the user's answers)

**Trigger**: The user has answered a prior QUESTIONS comment, either by
posting on the issue or by replying in the agent's chat session.

**Required action**:

1. Re-read the most recent unanswered QUESTIONS comment so the summary
   uses the same question numbers.
2. Post an **ANSWERS** comment using [`templates/comment-answers.md`](./templates/comment-answers.md).
   The body summarizes the user's answers in the agent's words — this
   produces a durable record on the issue thread even when the user
   answered in chat (which is otherwise invisible to the issue thread).
3. If the answers materially change the planned work, also note the
   change in the next commit message or PR description so the diff
   record reflects the new direction.

**Comment template**: `templates/comment-answers.md`.

**Always summarize, even if the user's answer was on the issue thread
itself.** The ANSWERS comment is what the agent is committed to — it
removes ambiguity about which interpretation the agent took.

### Behavior: PR-OPENED (a PR was opened for this issue)

**Trigger**: The agent has just created a pull request that addresses an
issue.

**Required action**:

1. Ensure the PR body contains `Closes #N` (or `Fixes #N`, `Resolves
   #N`) so GitHub auto-closes the issue on merge. This is the **only
   supported way** to tie a PR to its issue via the MCP — there is no
   dedicated tool.
2. Post a **PR-OPENED** comment on the issue using
   [`templates/comment-pr-opened.md`](./templates/comment-pr-opened.md).
3. After pushing and opening the PR, also follow the
   [`always-commit-skill-to-repo`](../always-commit-skill-to-repo/SKILL.md)
   and [`in-flight-workflow-tracking`](../in-flight-workflow-tracking/SKILL.md)
   conventions.

**Comment template**: `templates/comment-pr-opened.md`.

**One PR-OPENED comment per PR.** If a second PR is opened against the
same issue (e.g. the first was closed without merging), post a new
PR-OPENED comment naming the new PR; do not edit the old comment.

---

## The shared comment skeleton

Every issue-management comment uses **the same structural skeleton** so
the issue thread reads as a uniform timeline, but each type has a
**distinctive bold tag and one-line summary line** so a reader scanning
the thread sees the event types at a glance.

```markdown
> **[TAG]** — <one-line human-readable summary>
> <ISO-8601 UTC timestamp> · <event-specific metadata>

<body — content varies by type; see per-type template>

<sub>posted by the <code>issue-management</code> skill · <a href="#"><!-- machine marker --></a></sub>
<!-- issue-management:<event-tag>:v1 -->
```

Rules every comment template MUST follow:

1. **Opens with a blockquote** containing two lines: the tag line and the
   metadata line. Blockquotes render as a left-margin bar in the GitHub
   UI, giving every skill-emitted comment a recognizable silhouette and
   distinguishing it from free-form prose.
2. **The tag is in bold square brackets**, uppercase, no emojis. Tags in
   use: `[STARTED]`, `[QUESTIONS]`, `[ANSWERS]`, `[PR-OPENED]`. When the
   sub-skill adds a new behavior, the new tag goes here in this list.
3. **A short one-line summary** follows the tag on the same line,
   separated by `— `. The summary tells a thread-scanner *what* without
   forcing them to expand the comment.
4. **The body uses standard markdown** — no nested blockquote (which
   would visually conflate it with the header bar).
5. **Footer is a single HTML `<sub>` line plus a machine-readable HTML
   comment marker**. The marker lets future automation grep for, count,
   and validate skill-emitted comments. The marker form is exactly
   `<!-- issue-management:<event-tag>:v1 -->` so it survives copy-paste
   into anywhere markdown is rendered (the comment is invisible).
6. **No emojis.** This repo's style avoids them in committed artifacts.

The four templates below all share this skeleton; only the tag, summary,
and body fields differ. This gives the desired effect: visually
*similar* (same shape, same left bar, same footer), but *distinct at a
glance* (the bold tag and summary tell you immediately which kind of
event it is).

---

## Per-type quick reference

| Event | Tag | Trigger | Side effects on issue | Template |
|---|---|---|---|---|
| Picked up | `[STARTED]` | Agent picks up an unassigned issue | Assignee = `jonathanmanton`; `in-progress` label if it exists | [`templates/comment-started.md`](./templates/comment-started.md) |
| Asked | `[QUESTIONS]` | Agent needs user input | None | [`templates/comment-questions.md`](./templates/comment-questions.md) |
| Got answers | `[ANSWERS]` | User answered (here or in chat) | None | [`templates/comment-answers.md`](./templates/comment-answers.md) |
| PR exists | `[PR-OPENED]` | A PR addressing the issue was opened | PR body must say `Closes #N` | [`templates/comment-pr-opened.md`](./templates/comment-pr-opened.md) |

---

## Workflow (per event)

### STARTED

```text
1. read issue (issue_read get)
2. confirm unassigned; if assigned to someone else, ask the user before proceeding
3. read existing comments (issue_read get_comments); abort if a STARTED comment is already present
4. issue_write update assignees=["jonathanmanton"]
5. get_label("in-progress"); if found, issue_write update labels=[...existing, "in-progress"]
6. add_issue_comment using templates/comment-started.md
```

### QUESTIONS

```text
1. compose grouped, numbered questions
2. add_issue_comment using templates/comment-questions.md
3. if session may end before user answers: record in research/PLAN.md or IN-FLIGHT.md per in-flight-workflow-tracking
```

### ANSWERS

```text
1. find the prior QUESTIONS comment (issue_read get_comments)
2. compose a summary that reuses the same question numbering
3. add_issue_comment using templates/comment-answers.md
```

### PR-OPENED

```text
1. confirm PR body contains "Closes #N" (or "Fixes #N" / "Resolves #N") — update PR if it doesn't
2. add_issue_comment using templates/comment-pr-opened.md, naming the PR by number and URL
```

---

## Anti-patterns

- **Posting a STARTED comment for an issue you didn't actually claim.**
  The assignee update and the STARTED comment are a *pair* — do both or
  neither.
- **Posting QUESTIONS spread across multiple comments.** One batched
  numbered list per round-trip. The user can then answer with "1: foo,
  2: bar" without ambiguity.
- **Skipping the ANSWERS comment when the user answered in chat.** The
  issue thread has no idea what the user said in chat. Without the
  ANSWERS summary, the next agent (or the next human reader) sees only
  the question and no resolution.
- **Editing prior issue-management comments to "fix" what's now wrong.**
  Post a new comment instead. The append-only timeline is the point.
- **Adding emojis to make types more visually distinct.** Don't. The
  bold tag + blockquote bar is enough, and the repo's style avoids
  emojis.
- **Forgetting `Closes #N` in the PR body.** Without it, merging the PR
  does NOT close the issue automatically and you'll have to remember to
  close it manually — the PR-OPENED comment doesn't substitute.
- **Editing this `SKILL.md` directly to add a behavior.** Use the
  sibling [`add-issue-behavior`](../add-issue-behavior/SKILL.md) skill
  so the change is structured, the template is generated alongside, and
  the per-type quick-reference table stays in sync.

---

## Capability reference (for adding behaviors)

A summary of what the GitHub MCP integration can and can't do, so the
sub-skill (and you) know what's feasible when proposing a new behavior:

- **Can**: create / update / close issues; comment on issues and PRs;
  read issues, comments, labels, sub-issues; assign existing
  collaborators; apply *pre-existing* labels; create branches; create /
  update PRs (including body text, draft state, reviewers); merge PRs;
  enable / disable auto-merge.
- **Cannot via MCP**: create / edit / delete *labels*; list / create
  *milestones* (you can `issue_write milestone=N` if you already know
  the number); manage *issue types* (org-level admin); lock / pin /
  convert issues; mention people who aren't repo collaborators.
- **Via convention, not tool**: `Closes #N` / `Fixes #N` / `Resolves #N`
  in PR body to auto-close on merge; sub-issues to model parent/child
  via `sub_issue_write`.

If a proposed behavior depends on something in the **Cannot** column,
the sub-skill must either (a) downgrade the behavior to a comment-only
signal, (b) require the user to manually pre-create the label /
milestone, or (c) decline the behavior with a clear reason.

---

## Bundled files

- `templates/comment-started.md` — STARTED comment template.
- `templates/comment-questions.md` — QUESTIONS comment template.
- `templates/comment-answers.md` — ANSWERS comment template.
- `templates/comment-pr-opened.md` — PR-OPENED comment template.
- `templates/comment-skeleton.md` — the shared skeleton, used by the
  sub-skill when generating a new per-event template.
- `spec/FILLING-TEMPLATES.md` — concrete guidance on how to fill each
  placeholder in each template.

---

## See also

- [`add-issue-behavior`](../add-issue-behavior/SKILL.md) — the sub-skill
  for adding or changing a behavior in this skill.
- [`in-flight-workflow-tracking`](../in-flight-workflow-tracking/SKILL.md)
  — for cases where the agent posts a QUESTIONS comment and the session
  may end before the user answers.
- [`always-commit-skill-to-repo`](../always-commit-skill-to-repo/SKILL.md)
  — discipline that applies whenever this skill changes.
- [`github-connection-resilience`](../github-connection-resilience/SKILL.md)
  — what to do if a GitHub MCP call inside one of these behaviors fails.
