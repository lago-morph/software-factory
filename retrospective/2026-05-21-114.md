# Retrospective — Build the `issue-management` skill (conventions registry for GitHub issues)

- **UTC date**: 2026-05-21 (verified via `date -u +%Y-%m-%d` and `python3 -c "import datetime; print(datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%d'))"` — both returned `2026-05-21`)
- **Last PR**: #114 (highest PR number covered by this retro)
- **Branch at write time**: claude/issue-management-skill-M9IUN
- **Sibling artifacts**: [./2026-05-21-114/](./2026-05-21-114/)
- **Scope**: full session (from branch divergence) — no prior retrospective in this session

## Commit hashes by PR

### PR #114 — claude/issue-management-skill-M9IUN (merged 2026-05-21)

- `f15bd16` Add issue-management skill and add-issue-behavior sub-skill
- `0a02975` issue-management: dynamic identity + 5 new behaviors + label lifecycle
- `c4d4f8d` issue-management: embed modify-behavior as a mode; one distribution
- `58325e9` issue-management: restore looser modify-behavior triggers

PR-merge commit on `main`: `5c86065`.

## Part 1 — what happened

### Phase 1 — capability probe before design

The user asked for a skill that triggers whenever the agent works with a GitHub issue, codifying four required comment events (STARTED, QUESTIONS, ANSWERS, PR-OPENED). Before writing anything, the user explicitly directed me to "try to figure out what is allowed with the GitHub mcp server you are connected to" — can we create issues, comments, claim issues, label, tie PR to issue, etc.? I ran a battery of probes: `mcp__github__get_me`, `list_issue_types`, `get_label` for various default labels, and surveyed the available `mcp__github__*` tool schemas via ToolSearch. Findings: identity is `jonathanmanton` (the same human as the user — the MCP authenticates as them); `list_issue_types` returned 403; there is no `create_label` tool exposed; `Closes #N` in the PR body is the only convention-based way to link a PR closure to an issue closure. I reported the capability table to the user, then built v1.

### Phase 2 — initial skill with 4 behaviors

Built `.claude/skills/issue-management/SKILL.md` + 4 per-event templates + a shared `comment-skeleton.md` + `spec/FILLING-TEMPLATES.md` + a sibling `.claude/skills/add-issue-behavior/` skill that walks the user through adding new behaviors via a 6-step intake. Comment templates share a uniform skeleton (blockquote left bar + bold `[TAG]` + machine-readable HTML marker like `<!-- issue-management:STARTED:v1 -->`) so the issue thread is "visually similar but distinguishable at a glance." Hardcoded `jonathanmanton` as the assignee throughout (a mistake — see Phase 4). Pushed and opened PR #114 ready-for-review.

### Phase 3 — capability follow-ups + 3 new behaviors

User asked: does an assigned issue visibly change in the issue list? (Yes, but only via a small avatar — argues for also using a colored label.) Are there default labels created with a new repo? I probed all 9 GitHub defaults via `get_label` — all present in this repo. User then requested: use `good first issue` (repurposed from "good for newcomers" — a deliberate choice since `create_label` isn't available) as the claim marker; apply `question` label on QUESTIONS and remove on ANSWERS; remove `good first issue` on DUPLICATE/INVALID/WONTFIX closures but keep it for PR-merge/CLOSED-NO-PR; add DUPLICATE/INVALID/WONTFIX behaviors that close the issue and apply the right label; and crucially "**It should not hard code my name. It should query the mcp server for the identity used by the mcp server and use that.**" I rewrote STARTED to resolve identity via `get_me` at runtime, added the three new closure behaviors plus CLOSED-NO-PR and SUB-ISSUE-LINKED (the SUB-ISSUE-LINKED behavior was tricky because `sub_issue_write` needs the GitHub node ID, not the issue number), documented the label-lifecycle table, and updated the sub-skill's capability table to reflect the now-confirmed default labels. Posted two summary comments on the PR per user request: a "decisions made and why" comment, then a "what was created" comment.

### Phase 4 — embed the sub-skill as a mode (one distribution)

PR review comment from the user: "I wanted the sub skill to be embedded inside main skill so there is one distribution file to worry about. Should be triggered by 'I want to (action) issue behavior (name or synonym for behavior)' or semantically similar phrase." I consolidated: deleted `.claude/skills/add-issue-behavior/` entirely, moved its intake walk + capability table + worked example + anti-patterns into `issue-management/SKILL.md` as a new top-level **Modify-behavior mode** section (placed after the Capability reference). Updated the frontmatter `description` to advertise both trigger surfaces — primary mode and modify-behavior. Ran a quick `grep -rn "add-issue-behavior\|sibling"` before committing to confirm no stale cross-references survived.

### Phase 5 — trigger broadening

User flagged that I had over-narrowed the modify-behavior trigger surface: "I only wanted the more specific trigger phrase to invoke the sub skill. I want the trigger phrases we had for everything else. Maybe add this as a general trigger phrase also. But keep the old ones too." Fix: restored the original looser triggers ("add a behavior to the issue skill", "change the X behavior", "I want issues to also do Y", "the STARTED comment should also …", etc.) alongside the new canonical *"I want to (action) issue behavior (name)"* form. Also documented action-synonym variants (tweak/edit/revise/adjust/alter/drop/retire). Pushed `58325e9`. The PR was merged shortly after as `5c86065`.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 0 |
| PRs opened / merged | 1 / 1 |
| Real-world bugs discovered + fixed | 0 (no bugs; one trigger-surface drift caught in review) |
| Tests added (before / after) | 0 / 0 (no automated tests for skill markdown) |
| Spec amendments | 1 (`AGENTS.md` already covered draft-vs-ready policy; no changes needed) |
| Scenarios driven / skipped | N/A |
| Files touched at major refactors | 13 files across 4 commits — 1093 line insertions, 380 deletions across the SKILL.md + templates + spec |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `capability-probe-before-design` | SKILL-SPEC-6ffe696187 | high | MCP capability survey | [./2026-05-21-114/SKILL-SPEC-6ffe696187-capability-probe-before-design.md](./2026-05-21-114/SKILL-SPEC-6ffe696187-capability-probe-before-design.md) |

## Part 3 — agents-file suggestions

Proposed additions to `AGENTS.md`. Each rule has its own per-rule file in the sibling directory carrying both the `# agent instruction` body (what the CI/CD assembler concatenates) and the `# justification` (stripped by the assembler).

### Suggestion 1: Probe MCP capability before designing behaviors that depend on it — `AGENTS-MD-59a0fd420c`

- **Per-rule file**: [./2026-05-21-114/AGENTS-MD-59a0fd420c-probe-mcp-capability-before-designing-behaviors-that-depend-on-it.md](./2026-05-21-114/AGENTS-MD-59a0fd420c-probe-mcp-capability-before-designing-behaviors-that-depend-on-it.md)

**Proposed agent instruction:**

> **Probe MCP capability before designing behaviors that depend on it.** Before designing any skill behavior, automation, or convention that depends on an MCP server tool, perform a read-only probe of every required capability (e.g., `get_label`, `get_me`, `list_issue_types`) and document what is available, what is missing, and what is only achievable via convention. Design only what the probe confirms is feasible; downgrade or decline the rest.
>
> *Grounded in: PR #114, where probing `list_issue_types`, `get_label`, `create_label` (absent) up front killed bad behavior proposals before they reached the design doc.*

**Justification:**

When the user asked for an issue-management skill, the explicit first instruction was "try to figure out what is allowed with the GitHub mcp server you are connected to." That probe surfaced four hard constraints that shaped every behavior in the final skill: no `create_label` tool (forced repurposing `good first issue` instead of inventing `in-progress`), no `list_issue_types` access (forced using labels for type semantics), no `wontfix`/`invalid` state_reason enum (forced `not_planned` + label combinations), and identity-via-`get_me` rather than hardcoded login. Without the probe, all four would have been discovered as bugs *after* the design was committed to disk. The marginal cost was ten read-only MCP calls and one tool-list survey — under two minutes of agent time. The cost of skipping it is rework: every infeasible behavior consumes a full design-implement-discover-revert cycle, which in this session would have multiplied into hours.

### Suggestion 2: Never hardcode the MCP-authenticated identity — `AGENTS-MD-91e4d06700`

- **Per-rule file**: [./2026-05-21-114/AGENTS-MD-91e4d06700-never-hardcode-the-mcp-authenticated-identity.md](./2026-05-21-114/AGENTS-MD-91e4d06700-never-hardcode-the-mcp-authenticated-identity.md)

**Proposed agent instruction:**

> **Never hardcode the MCP-authenticated identity.** Skills, scripts, and configuration must not embed a literal GitHub login (or other MCP-resolved identity). Resolve it at runtime via `mcp__github__get_me` (or equivalent) and cache it for the session. Use a placeholder like `<mcp-login>` in documentation.
>
> *Grounded in: PR #114, where the user explicitly directed "it should not hard code my name; it should query the mcp server for the identity used by the mcp server and use that."*

**Justification:**

The first draft of `issue-management/SKILL.md` hardcoded `jonathanmanton` in seven places — assignee lists, capability tables, example commands. The user explicitly instructed: "It should not hard code my name. It should query the mcp server for the identity used by the mcp server and use that." This is a portability invariant, not a style preference: if the skill files are ever copied to a fork, used by a different MCP identity, or moved across orgs, every hardcoded login becomes a silent miscredit (assignments to a stranger, commits attributed to the wrong identity). The marginal cost of `get_me` is one MCP call cached for the session. The cost of every hardcode is a future correction commit plus whatever rot accumulated in between.

### Suggestion 3: Read the current label set before any GitHub MCP label-replace write — `AGENTS-MD-a41c70a86e`

- **Per-rule file**: [./2026-05-21-114/AGENTS-MD-a41c70a86e-read-the-current-label-set-before-any-github-mcp-label-replace-write.md](./2026-05-21-114/AGENTS-MD-a41c70a86e-read-the-current-label-set-before-any-github-mcp-label-replace-write.md)

**Proposed agent instruction:**

> **Read the current label set before any GitHub MCP label-replace write.** The MCP `issue_write update labels=…` REPLACES the entire label set. Before any label-modifying write, call `issue_read get_labels` to retrieve the current set, compute the new full set (union with additions, minus removals), and write back the merged list. Passing only the new labels wipes the existing ones.
>
> *Grounded in: PR #114 — the issue-management skill's QUESTIONS/ANSWERS/DUPLICATE/INVALID/WONTFIX behaviors all needed this discipline, and it was elevated to a top-level "anti-patterns" entry in the SKILL.md.*

**Justification:**

The GitHub MCP `issue_write update` operation has replace-not-merge semantics for labels. A naive "add the `question` label" call passing only `labels=["question"]` silently wipes every other label on the issue. The issue-management skill encountered this on five separate behaviors (QUESTIONS, ANSWERS, DUPLICATE, INVALID, WONTFIX), and the documented workflow for every one of them now starts with `issue_read get_labels`. This is not a one-skill concern — any future automation that touches GitHub labels via the MCP will hit it. The marginal cost is one extra MCP read call. The cost of skipping it is silent destruction of issue metadata, which is invisible until someone notices the missing labels weeks later.

### Suggestion 4: When extending a skill's trigger surface, add new phrases; never silently replace existing ones — `AGENTS-MD-ec255cf1b4`

- **Per-rule file**: [./2026-05-21-114/AGENTS-MD-ec255cf1b4-when-extending-a-skills-trigger-surface-add-new-phrases-never-silently-replace-existing-ones.md](./2026-05-21-114/AGENTS-MD-ec255cf1b4-when-extending-a-skills-trigger-surface-add-new-phrases-never-silently-replace-existing-ones.md)

**Proposed agent instruction:**

> **When extending a skill's trigger surface, add new phrases; never silently replace existing ones.** When the user proposes a new or more-specific trigger phrasing for an existing skill, ADD it to the trigger surface alongside the existing phrasings. Treat existing triggers as user-facing affordances: removing them without explicit instruction breaks discoverability for users who learned the old phrasings.
>
> *Grounded in: PR #114 — the user requested a more specific modify-behavior trigger; I over-narrowed and removed the looser phrasings; the user corrected: "I want the trigger phrases we had for everything else. Maybe add this as a general trigger phrase also. But keep the old ones too."*

**Justification:**

When I consolidated the `add-issue-behavior` skill into `issue-management` and adopted the user's new canonical *"I want to (action) issue behavior (name)"* phrasing, I dropped the original looser triggers ("add a behavior to the issue skill", "change the X behavior", "the STARTED comment should also …", etc.). The user noticed immediately and corrected. Trigger phrases are user-facing API: every existing phrasing represents a way the user (or future agents) might naturally express the request. Silently replacing them breaks an unobserved contract. The marginal cost of keeping the old phrasings is a few lines of description text. The cost of dropping them is a missed trigger plus a round-trip to re-add.

### Suggestion 5: Before deleting a named artifact, grep the repo for stale references — `AGENTS-MD-4bc01c7eec`

- **Per-rule file**: [./2026-05-21-114/AGENTS-MD-4bc01c7eec-before-deleting-a-named-artifact-grep-the-repo-for-stale-references.md](./2026-05-21-114/AGENTS-MD-4bc01c7eec-before-deleting-a-named-artifact-grep-the-repo-for-stale-references.md)

**Proposed agent instruction:**

> **Before deleting a named artifact, grep the repo for stale references.** Before deleting a skill directory, a template file, a script, a doc, or any other named artifact, run a recursive grep of the repository for references to the artifact's name (and any close synonyms). Update or remove every match before committing the deletion.
>
> *Grounded in: PR #114, where consolidating the `add-issue-behavior` skill into `issue-management` required a `grep -rn "add-issue-behavior\|sibling"` sweep to confirm no stale cross-references survived the delete.*

**Justification:**

When commit `c4d4f8d` removed the `add-issue-behavior` skill directory, the main `issue-management/SKILL.md` had several cross-references — `../add-issue-behavior/SKILL.md` links, "Invoke the sibling skill" anti-patterns, "See also" entries. A pre-delete grep caught all of them in one pass; without it, the merged PR would have shipped with broken links that fail silently until someone clicks. The marginal cost is one `grep -rn` invocation. The cost of skipping it is link rot that nobody notices until they need the link.

### Suggestion 6: Embed a machine-readable marker in every skill-emitted GitHub comment — `AGENTS-MD-db9a6b14a7`

- **Per-rule file**: [./2026-05-21-114/AGENTS-MD-db9a6b14a7-embed-a-machine-readable-marker-in-every-skill-emitted-github-comment.md](./2026-05-21-114/AGENTS-MD-db9a6b14a7-embed-a-machine-readable-marker-in-every-skill-emitted-github-comment.md)

**Proposed agent instruction:**

> **Embed a machine-readable marker in every skill-emitted GitHub comment.** Every comment that a skill posts on a GitHub issue or PR must end with an HTML comment marker of the form `<!-- skill-name:event-tag:vN -->` so future automation can grep, count, and validate skill-emitted comments. The marker survives copy-paste and is invisible in rendered markdown.
>
> *Grounded in: PR #114 — the `issue-management` skill's templates all end with `<!-- issue-management:TAG:v1 -->`, enabling future tooling to audit which agent-emitted comments exist and whether they used the current schema version.*

**Justification:**

GitHub issue threads grow long; distinguishing skill-emitted "agent-was-here" comments from free-form conversation by eye becomes impossible at scale. The marker `<!-- skill-name:event-tag:vN -->` is invisible in rendered markdown but trivially greppable. The version suffix lets a future schema migration distinguish v1 comments from v2 comments without false positives. The marginal cost is a single line per comment template. The cost of skipping it is that no future automation can reliably audit, count, or version-migrate skill output.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-0a329a8c05 | Repurpose `good first issue` label as the agent-claim marker | [./2026-05-21-114/ADR-0a329a8c05-repurpose-good-first-issue-label-as-the-agent-claim-marker.md](./2026-05-21-114/ADR-0a329a8c05-repurpose-good-first-issue-label-as-the-agent-claim-marker.md) |
| ADR-0d89e79e5e | Embed tightly coupled sub-skills as modes inside one SKILL.md | [./2026-05-21-114/ADR-0d89e79e5e-embed-tightly-coupled-sub-skills-as-modes-inside-one-skill-md.md](./2026-05-21-114/ADR-0d89e79e5e-embed-tightly-coupled-sub-skills-as-modes-inside-one-skill-md.md) |
| ADR-a1f4b82e27 | Resolve GitHub MCP identity at runtime via `get_me`, never hardcoded | [./2026-05-21-114/ADR-a1f4b82e27-resolve-github-mcp-identity-at-runtime-via-get-me-never-hardcoded.md](./2026-05-21-114/ADR-a1f4b82e27-resolve-github-mcp-identity-at-runtime-via-get-me-never-hardcoded.md) |
