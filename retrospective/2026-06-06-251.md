# Retrospective — discovery-phase planning arc: Board 1, the backbone build plan, and the handoff rotation

- **UTC date**: 2026-06-06 (verified via `date -u` and `python3 datetime`, which agreed)
- **Last PR**: #251 (highest PR number covered by this retro)
- **Branch at write time**: claude/software-factory-c07-cards-ahWyY
- **Sibling artifacts**: [./2026-06-06-251/](./2026-06-06-251/)
- **Scope**: full session (from branch divergence) — no prior retrospective was committed during this session

## Commit hashes by PR

### PR #249 — claude/software-factory-c07-cards-ahWyY (merged 2026-06-06)

- `1a0c9c0` Add Board 1 — the opening play menu (handoff Step 1)

### PR #250 — claude/software-factory-c07-cards-ahWyY (merged 2026-06-06)

- `86e1829` Add backbone implementation plan (handoff Step 2)

### PR #251 — claude/software-factory-c07-cards-ahWyY (merged 2026-06-06)

- `2b9d457` Rotate session handoff: board + backbone plan closed; next is execution

*(All three rode the one designated session branch, recreated off `main` after each merge — see
Suggestion 5.)*

## Part 1 — what happened

This was a **planning/authoring session** that drove the discovery phase's three-step arc from "no
factory artifacts beyond the charter" to "board + build plan + rotated handoff, all merged." No code, no
subagents, no running software — the deliverables were operator-facing documents, and the lessons are
about process, environment, and getting operator decisions right.

### Phase 1 — Board 1 (handoff Step 1)

Goal: author the opening **board of cards** — the play menu the operator shops by mood — and it had to
include the C07 glossary self-build. I read the charter, next-steps, methodology, and the build order to
ground the card flavors, then tried to surface the two operator decisions the handoff flagged
(real-driver strategy; portfolio scope) via `AskUserQuestion`. **The popup did not render for the
operator** — they replied that the UI showed nothing and asked for the choices in plain language. I
explained inline; the operator then gave decisive direction: Board 1 is a **shakedown cruise** to see
whether the backbone-25 work together, real drivers are **factory self-builds only**, agent-os appears
**only as reduced-fidelity models**, the portfolio is frozen, and a **bead TUI viewer** toy was added. I
authored [`BOARD.md`](../BOARD.md) at repo root, wired it into AGENT-ENTRY §2, validated links, and
PR'd it. Merged.

### Phase 2 — The backbone build plan (handoff Step 2)

Goal: plan the build of the 25 backbone components, "some install-and-configure, some custom." I pulled
the binding inputs (the build order, the rules of the game, the **8 panel amendments**, the panel-vetted
macro-shape, the open decisions D-20/#4). A naming hazard surfaced: the existing **unified plan** covers
*exercising* the factory, not *building* it — so I deliberately authored
[`backbone-implementation-plan.md`](../architectures/v4/backbone-implementation-plan.md) as a distinct
artifact that **ends where the unified plan begins** (the Gate-B4 seam). It leads with the adopt-vs-build
split (16 adopt / 9 build), sequences five gates (B0 substrate-truth → B4 shakedown), and maps every
rule-of-the-game and every amendment to the gate that enforces it. Validated the gate diagram via the
mermaid tool (which overflowed — see Suggestion 3), checked links, PR'd. Merged.

### Phase 3 — Handoff rotation + the environment-reality clarification

The operator asked the pivotal question: *"you are going to build all this in the agent sandbox?"* I
answered honestly — the ephemeral, network-gated web sandbox is for authoring and committing, **not** for
hosting a running factory; Step 3 (the Gas City conformance check on a real pinned `gc`) needs a
persistent operator-controlled host. The operator then said "update handoff for us to do the next steps
in a new session." I authored a new dated handoff
([`SESSION-HANDOFF-2026-06-06-…`](../architectures/v4/SESSION-HANDOFF-2026-06-06-board-and-backbone-plan-close.md))
carrying that environment reality as the load-bearing fact, repointed AGENT-ENTRY §2 (and found **two**
stale "active handoff" markers to demote — Suggestion 4), and added task-aware reading lists. Merged on
the operator's "merge and retrospect."

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 0 |
| PRs opened / merged | 3 / 3 (#249, #250, #251) |
| Real-world bugs discovered + fixed | 0 (planning session; no code) |
| Tests added (before / after) | 0 / 0 |
| Spec amendments | 0 (consumed the 8 panel amendments as constraints; authored none) |
| Scenarios driven / skipped | 0 / 0 |
| Files touched at major refactors | 3 new docs + AGENT-ENTRY.md edited across all 3 PRs |

## Part 2 — skills summary

**No skill candidates this session.** The work exercised existing patterns (operator-facing doc
authoring, the commit/PR loop, handoff rotation per the autonomous-run conventions) with no novel
orchestration that a fresh skill would capture. The durable harvest here is **agents-file rules** (Part
3) and **proposed ADRs** (Part 4). Proposing a skill on this evidence would be speculative — deliberately
omitted.

## Part 3 — agents-file suggestions

Proposed additions to `AGENTS.md`. Each rule has its own per-rule file in the sibling directory
(`# agent instruction` = the verbatim text the CI/CD assembler concatenates into `AGENTS.md`;
`# justification` = the persuasion the assembler strips). Inlined here for review convenience.

### Suggestion 1: Carry decision substance in chat, not only in AskUserQuestion — `AGENTS-MD-f2885f08fd`

- **Per-rule file**: [./2026-06-06-251/AGENTS-MD-f2885f08fd-carry-decision-substance-in-chat-not-only-in-askuserquestion.md](./2026-06-06-251/AGENTS-MD-f2885f08fd-carry-decision-substance-in-chat-not-only-in-askuserquestion.md)

**Proposed agent instruction:**

> **Carry decision substance in chat, not only in AskUserQuestion.** When you put a decision to the operator via `AskUserQuestion`, also state the choices and their trade-offs in plain prose in the same chat turn — never rely on the question UI alone to carry the substance. If the operator declines the question, proceed with the recommended default and surface the open decision inside the deliverable rather than re-asking.
>
> *Grounded in: the operator's AskUserQuestion popup did not render — "the UI didn't show your explanation; tell me in plain language what the choices are".*

**Justification:** The `AskUserQuestion` popup failed to render for the operator this session: they
replied "The user interface didn't show your explanation. Tell me in plain language what the choices
are." The two flagged decisions (real-driver strategy, portfolio scope) were entirely inside the
unrendered widget, so the operator was momentarily blocked on a question they couldn't see. The cost of
the rule is one extra paragraph of prose per question; the cost of omitting it is a stalled turn and an
operator who can't act. The asymmetry is stark — always duplicate the choice substance in chat text.

### Suggestion 2: Known check-internal-refs BARE_TEXT false positives — `AGENTS-MD-7325090972`

- **Per-rule file**: [./2026-06-06-251/AGENTS-MD-7325090972-known-check-internal-refs-bare-text-false-positives.md](./2026-06-06-251/AGENTS-MD-7325090972-known-check-internal-refs-bare-text-false-positives.md)

**Proposed agent instruction:**

> **Known `check-internal-refs.py` BARE_TEXT false positives.** The repo's `scripts/check-internal-refs.py` BARE_TEXT heuristic flags descriptive link-text phrases (e.g. "the next-steps report", "the binding spec") even when they are already inside a well-formed markdown link. These are false positives consistent with the existing corpus: verify the link has a real, resolving target, then move on. Do NOT rewrite good descriptive link text to silence the heuristic, and do NOT treat a nonzero finding count as failure — the checker exits 0 by design.
>
> *Grounded in: the same BARE_TEXT phrases recurred on BOARD.md, the backbone plan, and the handoff across PRs #249/#250/#251, all inside valid links.*

**Justification:** Across all three PRs this session, the link checker flagged phrases like "the
next-steps report" and "the binding spec" that were already proper `[text](path)` links — the identical
pattern the merged charter and methodology docs use. Each time I confirmed the link target resolved and
left the text alone. Without this rule a future agent burns a review cycle "fixing" correct,
corpus-consistent link text to appease a heuristic that exits 0 anyway; with it, the agent runs one
`ls`/grep on the target and proceeds. The checker is advisory — knowing which of its findings are noise
is the whole value.

### Suggestion 3: Extract only valid from the mermaid validator output — `AGENTS-MD-3c62be736a`

- **Per-rule file**: [./2026-06-06-251/AGENTS-MD-3c62be736a-extract-only-valid-from-the-mermaid-validator-output.md](./2026-06-06-251/AGENTS-MD-3c62be736a-extract-only-valid-from-the-mermaid-validator-output.md)

**Proposed agent instruction:**

> **Extract only `.valid` from the mermaid validator output.** The `validate_and_render_mermaid_diagram` tool returns a payload with embedded SVG and PNG that can exceed the inline token limit; do not read its result inline. When it reports the output was saved to a file, run `jq '{valid, diagramType}'` on that file for the verdict instead.
>
> *Grounded in: validating a single 6-node flowchart returned ~143k characters and overflowed the tool result.*

**Justification:** Validating one small (6-node) flowchart produced a ~143,000-character result because
it embeds both a rendered SVG and a base64 PNG. Read inline, that blows the result budget and pollutes
context for no benefit — all I needed was the boolean `valid`. The fix is a single `jq` call on the
saved result file. One tool call versus a 143k-token dump: adopt the rule and never read a mermaid render
inline.

### Suggestion 4: Demote every prior active-handoff marker when rotating the handoff — `AGENTS-MD-e14d284de2`

- **Per-rule file**: [./2026-06-06-251/AGENTS-MD-e14d284de2-demote-every-prior-active-handoff-marker-when-rotating-the-handoff.md](./2026-06-06-251/AGENTS-MD-e14d284de2-demote-every-prior-active-handoff-marker-when-rotating-the-handoff.md)

**Proposed agent instruction:**

> **Demote every prior active-handoff marker when rotating the handoff.** When you write a new SESSION-HANDOFF and repoint AGENT-ENTRY §2, grep the current-state list for every existing "active handoff" marker and demote ALL of them to "superseded …" — there must be exactly one active handoff pointer. Do not assume only the immediately-prior handoff is marked active.
>
> *Grounded in: AGENT-ENTRY §2 carried two stale "active handoff" markers (the 2026-06-05 v4 handoff and the v3 Phase-8 handoff) at rotation time.*

**Justification:** When rotating the handoff this session I found AGENT-ENTRY §2 already claimed TWO
active handoffs — the 2026-06-05 v4 one and a leftover v3 Phase-8 one — so "where are we?" had two
contradictory answers. Repointing only the obvious one would have left three. The rule costs one `grep
"active handoff"` over the file; skipping it leaves a navigation doc that lies about the current pickup
point, which is exactly the doc a fresh session trusts first.

### Suggestion 5: Reposition a merged session branch before the next deliverable — `AGENTS-MD-cd020482f4`

- **Per-rule file**: [./2026-06-06-251/AGENTS-MD-cd020482f4-reposition-a-merged-session-branch-before-the-next-deliverable.md](./2026-06-06-251/AGENTS-MD-cd020482f4-reposition-a-merged-session-branch-before-the-next-deliverable.md)

**Proposed agent instruction:**

> **Reposition a merged session branch before the next deliverable.** After a mid-session PR merges, do not stack new commits on the pre-merge branch tip. Fast-forward the designated session branch onto current `main` first — `git stash push -u`, `git fetch origin main`, `git merge --ff-only origin/main`, `git stash pop` — then commit the next deliverable and open a fresh PR. Verify the prior work is actually in `main` (`git log --oneline origin/main`) before relying on it.
>
> *Grounded in: three sequential deliverables (board, backbone plan, handoff) each needed the branch fast-forwarded onto the freshly-merged main before the next commit.*

**Justification:** This session shipped three deliverables in series, and each time the prior PR merged
and its branch was deleted, leaving the local branch pointing at a now-merged commit. Committing the next
deliverable on that stale tip would have produced a PR whose diff re-included already-merged files (or a
confusing merge base). The stash/ff/pop sequence keeps the branch a clean single-commit delta on top of
live `main`. It is four git commands at each boundary versus a tangled, hard-to-review PR — cheap
insurance that complements the existing fresh-PR-after-merge rule with the concrete mechanic.

### Suggestion 6: Docs-only PRs trigger no CI in this repo — `AGENTS-MD-56e288c390`

- **Per-rule file**: [./2026-06-06-251/AGENTS-MD-56e288c390-docs-only-prs-trigger-no-ci-in-this-repo.md](./2026-06-06-251/AGENTS-MD-56e288c390-docs-only-prs-trigger-no-ci-in-this-repo.md)

**Proposed agent instruction:**

> **Docs-only PRs trigger no CI in this repo.** This repo's GitHub Actions workflows are all path-scoped (e.g. `architectures/0N-*.md`, `sources.json`, research-pipeline scripts). A PR that touches only documentation (root `*.md`, `architectures/v4/**`, `AGENT-ENTRY.md`) produces zero check runs, so a `pending`/0-checks status is the terminal state, not a queue to wait on. Confirm via `actions_list` / `get_check_runs` and do not arm a CI wait for such a PR.
>
> *Grounded in: PRs #249/#250/#251 each returned 0 check runs; listing the 5 repo workflows confirmed none match docs paths.*

**Justification:** Subscribed to three docs-only PRs this session, each showing combined status `pending`
with zero checks. That looks like "CI is still queued" but it is actually "no workflow matches these
paths" — a terminal state. Listing the five workflows confirmed all are path-scoped away from docs.
Without this rule an agent waits (or worse, polls) indefinitely for checks that will never appear; with
it, one `get_check_runs` plus a glance at the workflow paths settles it and the agent moves on.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-737b450949 | Factory execution runs on a persistent operator-controlled host, not the ephemeral web sandbox | [./2026-06-06-251/ADR-737b450949-factory-execution-runs-on-a-persistent-operator-controlled-host-not-the-ephemeral-web-sandbox.md](./2026-06-06-251/ADR-737b450949-factory-execution-runs-on-a-persistent-operator-controlled-host-not-the-ephemeral-web-sandbox.md) |
| ADR-925e61d87c | The backbone build plan and the exercising plan are distinct artifacts meeting at the Gate-B4 seam | [./2026-06-06-251/ADR-925e61d87c-the-backbone-build-plan-and-the-exercising-plan-are-distinct-artifacts-meeting-at-the-gate-b4-seam.md](./2026-06-06-251/ADR-925e61d87c-the-backbone-build-plan-and-the-exercising-plan-are-distinct-artifacts-meeting-at-the-gate-b4-seam.md) |

One-line rationale per draft appears in each draft's `## Context` opening sentence.
