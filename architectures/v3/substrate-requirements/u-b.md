# Substrate requirements — U-B (Unified-attempt, Pace-Layered Escrow Factory)

**Candidate.** [U-B — Pace-Layered Escrow Factory (5-layer artifact stack with bidirectional traversal)](../tracks/unified-B.md). Mandate: unified-attempt. Axis: pace-layer × bidirectional traversal (per [registry §U-B](../candidate-registry.md#u-b--pace-layered-escrow-factory-5-layer-artifact-stack-with-bidirectional-traversal)).

**Phase-3.5.5 status.** `survives with deferred-defense flag` (per [registry §U-B post-smoke-test](../candidate-registry.md#u-b--pace-layered-escrow-factory)). The Phase-3.5 follow-up smoke-test [`P-31-smoke-test-invariants.md`](../primitives/P-31-smoke-test-invariants.md) produced 5/5 non-trivial cross-layer invariants per [`auto-002` Round 2](../decisions/auto-002-ub-path.md). Forward action: full Phase-4 invariant-authoring sub-track authorized (Wave 4.5 — see §5).

## §1 Primitive list (buildability-confirmed)

U-B requires 4 substrate primitives (cognitive-escrow demoted to methodology-layer per [DEC-2](../phase-3.4-decisions-resolved.md#dec-2--cognitive-escrow-placement-methodology); not listed here).

- **[P-28 — Typed-object store (layer-typed envelope variant)](../primitives/P-28-typed-object-store.md).** One typed-object store keyed primarily on `layer` ∈ {L0 Standards, L1 Architecture, L2 Spec, L3 Plan, L4 Code} with the `TypedObject<L>` envelope carrying `change-rate`, `escrow-policy`, `invariants[]`, `parent-layer-ref`, `child-layer-refs[]` per [P-28 sketch § U-B layer-typed envelope](../primitives/P-28-typed-object-store.md). The envelope is **layer-indexed rather than interval-indexed** (the differentiator vs U-A's variant). Verdict: `designed-system`; envelope-schema design content is U-B-specific. Same-vs-distinct vs other P-28 variants deferred to Phase 4.2.
- **[P-29 — Policy mediator / transition gates (per-layer-boundary variant)](../primitives/P-29-policy-mediator.md).** Gates each Lᵢ → Lᵢ₊₁ transition with a declarative-policy mediator. Closure conditions encode the boundary-specific `escrow-policy` field of the upstream typed-object (per [P-28 sketch § U-B](../primitives/P-28-typed-object-store.md)); the mediator refuses to materialize the downstream-layer object until the upstream's gate, log, judge-diversity, and watchdog-tier declarations have satisfying records. Verdict: `designed-system`. Same-vs-distinct vs U-A's policy mediator and D7-U-1's compounding gate deferred to Phase 4.2.
- **[P-31 — Cross-layer drift detector](../primitives/P-31-cross-layer-drift-detector.md).** Holds typed-object snapshots of every layer artifact and runs a per-layer-pair invariant checker across L0↔L1, L1↔L2, L2↔L3, L3↔L4 (and the long-distance L0↔L4 anchor-to-implementation pair). Emits `LayerDriftEvent` to Patrol (P-06) and to the U-B escrow primitive (methodology layer per DEC-2). Load-bearing for F34 (cross-layer drift) per [unified-B §2.5](../tracks/unified-B.md#25-key-failure-mode-coverage-greenfieldbrownfield-severity-aware). Verdict: `research-grade-uncertainty` — see §2 for treatment.
- **[P-14 — Judge router (per-layer routing variant)](../primitives/P-14-judge-router.md).** Layer-aware multi-shape judge dispatch: L0/L1 long-context + diverse families; L2→L3 cross-family contradiction-detection; L4 provider-aligned coding agents (per [P-14 sketch § U-B](../primitives/P-14-judge-router.md)). Verdict: `designed-system`. Not contested-fixed-header.

## §2 RG primitives

Per the [Phase-3.5.5 RG-primitive rule](../candidate-registry.md#phase-355-rule-on-load-bearing-rg-primitives-binding-user-approved-2026-05-25), U-B carries **two RG-portion entries** in the [Application to current candidates table](../candidate-registry.md#phase-355-rule-on-load-bearing-rg-primitives-binding-user-approved-2026-05-25). Both verbatim text-pulls per Reviewer 1 A3:

> | U-B | P-31 (full primitive) | (a) bounded sub-track | Smoke-test passed; full sub-track authorized at Phase 4 (scale 1-per-pair to ≥3-per-pair, ≥15 total) |

> | U-B | P-31 long-distance pair (L0↔L4 judge-arm) | (b) accept-as-RG (carry-from-smoke-test caveat) | The smoke-test's L0↔L4 invariant depends on LLM-judge for substance check; treated as accept-as-RG per the smoke-test's own caveat. Full sub-track explores whether deterministic alternatives exist; falls back to accept-as-RG if not. |

**Choice treatment per [auto-002 Round 2](../decisions/auto-002-ub-path.md).**

- **P-31 full primitive → (a) bounded sub-track.** The Phase-3.5 follow-up smoke-test [`P-31-smoke-test-invariants.md`](../primitives/P-31-smoke-test-invariants.md) attempted 1 non-trivial machine-checkable cross-layer invariant per layer-pair and produced 5/5 non-trivial invariants with verbatim corpus citations (L0↔L1 AILCCP three-controls coverage per Kahana 31; L1↔L2 `protects` linkage + binding-shape match per El Kaim Ch8; L2↔L3 chunk-load ceiling at Yang et al. F36 empirical 10-requirement bar; L3↔L4 expected-touch dependency-closure containment per F34; L0↔L4 AILCCP-control runtime-mode shape match per Kahana "trappings vs substance"). Per the smoke-test verdict logic (≥4 of 5 → survives, full sub-track authorized), the full Phase-4 invariant-authoring sub-track is authorized and **owed at Wave 4.5** (scale recipe to ≥3 per pair, ≥15 total).

- **P-31 L0↔L4 judge-arm → (b) accept-as-RG.** The strongest invariant by corpus anchor (Kahana 31 §5 "trappings vs substance") relies on an LLM-judge arm whose reliability is bounded by F37/F51 (Larbi MCC ≤ 0.55). The deterministic-reachability arm alone is non-trivial; the *substantive* runtime-mode shape-match arm is research-grade. P-31's Construction-C hybrid (deterministic + judge residue routed to P-14 panel) is the right substrate but does not eliminate the RG inheritance. The smoke-test's own §6.3 caveat flags this; the Wave 4.5 full sub-track explores whether deterministic alternatives exist and falls back to accept-as-RG if not.

## §3 Candidate-specific contracts on each primitive

U-B references two contested primitives (P-28, P-29) plus P-31 and P-14 (not contested-fixed-header).

- **P-28 layer-typed store.** envelope schema: `TypedObject<L>` per-pace-layer hierarchy with fields `{layer ∈ {L0 Standards, L1 Architecture, L2 Spec, L3 Plan, L4 Code}, change-rate, escrow-policy, invariants[], parent-layer-ref, child-layer-refs[]}` per [P-28 sketch § U-B layer-typed envelope](../primitives/P-28-typed-object-store.md). Typed filter keyed on `layer` first; the `(parent-layer-ref → child-layer-refs)` traversal is P-31's primary input axis. Differentiator: layer-indexed (not interval-indexed like U-A; not anchor-immutability-indexed like U-C; not FC-commitment-indexed like D7-U-1). Same-vs-distinct deferred to Phase 4.2.

- **P-29 transition gates.** policy DSL: per-layer-boundary closure rules. For each Lᵢ → Lᵢ₊₁ pair, the Rego policy encodes the upstream layer's `escrow-policy` field as a closure condition: the downstream-layer object cannot materialize until the upstream's declared boundary checks (cost-ceiling per D-5; holdout-discipline per D-4 at L3→L4; cross-family contradiction-detection at L2→L3; AILCCP delegation-level confirmation at L0→L1 and L1→L2) have satisfying records. Differentiator: per-layer-boundary DSL (not interval-closure DSL like U-A; not FC-survival DSL like D7-U-1). Same-vs-distinct deferred to Phase 4.2.

- **P-31 cross-layer drift detector.** No fixed contested-primitive header — U-B is the sole claimant. Contract: reads the P-28 layer-typed store via layer-pair queries, evaluates per-layer-pair invariants (catalog is the Wave 4.5 deliverable), emits `LayerDriftEvent{layer-pair, invariant-id, drifted-artifact-handle, severity, recommended-handback-layer}` to Patrol and to the U-B methodology-layer escrow primitive. Construction-C hybrid per [P-31 sketch](../primitives/P-31-cross-layer-drift-detector.md): deterministic OPA + Postgres CTE + property tests for declared-invariant arms; LLM-judge residue via P-14 for substance-check arms (F37 unreliability inheritance per §2).

- **P-14 per-layer judge router.** No fixed contested-primitive header. Layer-aware routing is U-B's contract per [P-14 sketch § U-B](../primitives/P-14-judge-router.md): per-layer provider-property requirements as a registry.

## §4 X_UNM_B articulation

Per the [X_UNM_B finding](../candidate-registry.md#u-a--escrow-graph-factory-cycle--directed-graph-of-typed-nodes), this candidate addresses Codebase-Model acquisition from legacy artifacts as follows:

- **Source artifact:** for brownfield, U-B reads L4 code (the existing codebase) as the entry artifact and performs bottom-up inference of L3 plan / L2 spec / L1 architecture / L0 standards through code analysis (per [unified-B §1 § Brownfield cycle](../tracks/unified-B.md) — *"L4 (existing code) → L3 (inferred plan via trace analysis) → L2 (inferred delta-spec) → L1 (inferred existing architecture) → L0 (inherited standards) → downward change traversal against the inferred upper stack"*). Inherited regulatory commitments (F58) seed L0 directly.
- **Extraction primitive:** primarily **P-22 polyglot codebase index** + **P-23 dependency-and-impact graph** + **P-26 conventional view (CONDITIONAL on BF-L's smoke-test result)** + ADR archaeology of any preserved historical documents. The bottom-up inference chain composes these: P-22 indexes symbols; P-23 yields the structural skeleton for the L1 architecture inference; P-26's conventional view (if BF-L's Wave-4.5 conventional-view smoke-test authorizes its full sub-track per [auto-003](../decisions/auto-003-bfl-rg-view-choice.md)) provides per-language idiomatic patterns that the L2 spec inference reads. If BF-L's conventional view lands accept-as-RG, U-B inherits the same RG status for its L2 inference accuracy.
- **Completeness gap:** L0/L1/L2 reconstruction from L4 is **fundamentally lossy**. L0 standards (regulatory commitments, AILCCP control declarations, Caremark RSI three-part test status) are not derivable from code in general — they are extrinsic governance artifacts; only the *implementation tags* of L0 commitments are recoverable from L4 (and only when those tags are preserved as `@implements-AILCCP("...")` annotations or equivalent — the very thing U-B's L0↔L4 invariant L0-L4-1 in [`P-31-smoke-test-invariants.md` §5](../primitives/P-31-smoke-test-invariants.md) would *check*, not produce). L1 architecture inference recovers structural shape from P-23 but not architectural-intent (the *why* of a layering choice). L2 spec inference recovers behavioral surface from tests + types but not specification-intent (the El Kaim 9-field intent block is not in the code).
- **Fallback if missing:** if upward inference fails or produces low-confidence outputs for L0/L1, U-B degrades to **greenfield-only**: the candidate cannot serve the brownfield mandate. This is the honest accounting per [unified-B §6 § Not trying to be the strongest brownfield-only architecture](../tracks/unified-B.md#6-what-this-track-is-not-trying-to-be) (*"PLEF's bottom-up traversal is competent at brownfield, not optimal"*). The degradation does not invalidate U-B's unified claim because the load-bearing falsification (UC4's stronger reading) applies in the domain where pace-layer + escrow is the natural organizing surface; the candidate explicitly does not claim global UC4-resolution.

## §5 Open carries

- **Phase-4-internal workstreams.** **U-B invariant-authoring sub-track — Wave 4.5 (OWED).** Per [auto-002 Round 2](../decisions/auto-002-ub-path.md) and [auto-004 Round 2 Wave 4.5](../decisions/auto-004-phase-4-dispatch-shape.md#wave-45-new-per-reviewer-2-a3): scale the [smoke-test recipe](../primitives/P-31-smoke-test-invariants.md) from 1-per-pair to ≥3-per-pair, ≥15 total. Carry the smoke-test caveats (per [registry §U-B forward action](../candidate-registry.md#u-b--pace-layered-escrow-factory) and [smoke-test §6.3](../primitives/P-31-smoke-test-invariants.md)): (a) **sample-size bias** — the smoke-test produced 1 per pair; the gradient from 1 to ≥3 is non-trivial and some pairs (notably L2↔L3 and L3↔L4) may not scale beyond 2 corpus-anchored invariants; (b) **L0↔L4 judge-arm RG inheritance** — the strongest invariant leans on LLM-judge for substance check; this portion remains accept-as-RG per §2; (c) **corpus concentration on AILCCP / EARS / El-Kaim-Ch8** — these are the three richest corpus sources; the sub-track is *permitted* to draw heavily on them and is not required to source-diversify if that would mean fabrication; (d) **OQ-PLEF-3 multi-cycle population drift is out of scope** for Wave 4.5 and may be deferred to Phase 5 ADR or accept-as-RG at Phase 4 close. **Fallback rule:** if the full sub-track fails to scale to ≥3 per pair, fall back to the smoke-test's 1-per-pair set with explicit accept-RG flag on the substantive-drift portion (per [registry forward action](../candidate-registry.md#u-b--pace-layered-escrow-factory)).

- **Phase-5 ADR seeds.** (i) P-28 layer-typed envelope schema versioning (how does layer-count migration interact with the typed-object store? — OQ-PLEF-1); (ii) P-29 transition-gate policy DSL (declarative-language choice — OPA vs Cedar — and per-layer policy-bundle distribution); (iii) P-31 Construction-C hybrid composition rule (deterministic vs LLM-judge residue threshold and confidence-routing — touches the L0↔L4 accept-RG portion); (iv) P-14 per-layer routing registry shape (how per-layer provider-property requirements are versioned alongside L0 standards).

- **Phase-8 lean-eval candidates.** (i) Wave-4.5 sub-track output pressure-test — measure cross-layer drift detection MCC on a held-out drift corpus per [P-31 sketch §Falsifiability](../primitives/P-31-cross-layer-drift-detector.md); (ii) L0↔L4 judge-arm reliability under F37 (cross-family panel with confidence threshold) — does the substantive shape-match arm meet operator-meaningful precision/recall? (iii) brownfield bottom-up inference accuracy under §4's completeness-gap — what fraction of L1/L2 inference loss can U-B detect-and-flag rather than silently fail-on?

## §6 Scoping-principle compliance

This summary preserves U-B as a defensible architecture proposal:

- No primitive has been pre-eliminated; all 4 substrate primitives carry forward.
- The load-bearing RG primitive (P-31) is honestly surfaced with **both** its (a) bounded-sub-track and (b) accept-as-RG portions verbatim from the Phase-3.5.5 application table, and routed to Wave 4.5 with explicit smoke-test caveats. The smoke-test's 5/5 non-trivial-invariant result is the deferred-defense evidence U-B carries into Phase 4; the deferred-defense flag's gate is Wave 4.5.
- The X_UNM_B completeness gap (§4) is honestly accounted: L0/L1/L2 reconstruction from L4 is fundamentally lossy, and U-B explicitly declares the greenfield-only degradation rather than overclaiming brownfield-fit.
- Open methodology questions (layer-count empirical-not-derived per OQ-PLEF-1, pace-layer's own F52 risk per OQ-PLEF-8, multi-cycle drift per OQ-PLEF-3) are routed to Phase-5 ADR / Phase-8 lean-eval / accept-as-RG rather than used to demote the candidate.
- Same-vs-distinct verdicts on P-28 / P-29 variants are deferred to Phase 4.2 per the dispatch-brief constraint; this summary does not pre-empt them.

U-B survives Phase-4.1 with the deferred-defense flag intact (per [registry status](../candidate-registry.md#u-b--pace-layered-escrow-factory)). **Wave 4.5 invariant-authoring sub-track is the deferred-defense gate.**
