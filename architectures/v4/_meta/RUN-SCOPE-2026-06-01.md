# Scope envelope — Sweep-2 spine run (2026-06-01)

**Author.** Lead agent, autonomous-run session 2026-06-01.
**Status.** Operator on a plane / unreachable → proceeding on implicit confirmation (envelope is the fallback contract). Grounded in [`HANDOFF.md` §0](./HANDOFF.md), the build order [`implementation-dependencies.md`](../implementation-dependencies.md), the coverage ledger [`STATUS.md`](./STATUS.md), and the decision/OQ work list [`review-log.md`](./review-log.md).

This document aligns intent before the unattended run. It is the contract: what the run produces, what it will *not* touch, the first decision forks and how I'll resolve them, and the stop conditions. The morning reviewer reads against this envelope.

---

## What I plan to do

Author **Sweep-2 implementation-ready depth** for the **25-component safe-self-build spine** (the 7 products in [`HANDOFF.md` §0.2](./HANDOFF.md)), in product order, deepening **in place** (preserving Sweep-1 content + `[D-23 substrate-verified]` + `[D-30 ADOPTED]` annotations + inline OQs), to the [§0.4 depth bar](./HANDOFF.md) (concrete signatures, data schemas, API/message/**config** contracts, valid Mermaid `stateDiagram-v2`/`sequenceDiagram`, E-code error taxonomies, AC-code acceptance tests cross-referencing the E-codes), with the [`C20-bead-schema.md`](../spec/C20-bead-schema.md) spec as the format exemplar.

- **Gas City integration product FIRST + deepest** — the 11 components (C01, C02, C03, C04, C05, C17, C18, C42 newly deepened; C19/C23/C41 seam-refined) as one **install + config** product, grounded in `lago-morph/gascity-prototype` + the [D-23 substrate harvest](./D-23-substrate-harvest.md), PLUS the concrete **integration runbook** (`city.toml`/`pack.toml`/`.gc/site.toml`) and the **Gas City conformance check** (the [D-23 spike protocol](./D-23-gas-city-spike-protocol.md) operationalized). Its own stacked PR(s). *This is the piece the operator implements first.*
- **Claude Code / Max product** — **C28** (the `claude` worker under Max) + **C29** (cost/family routing policy), plus the **Max integration runbook** (provider preset, `CLAUDE_CODE_OAUTH_TOKEN`/CA-bundle/`IS_SANDBOX=1`/onboarding pre-acks, grounded in the prototype). Its own stacked PR. *Second priority — operator also starts this first.*
- **Spec intake** (C08 spec format, C09 prompt binding) + **Bead-type schema seam-refine** (C20). Its own stacked PR.
- **Evaluation tier on Inspect AI** (C30 scenario store, C31 runner, C32 judge, C33 satisfaction reduction). Its own stacked PR.
- **The fence** (C34 holdout integrity, C43 boundary-typing half — gated on D-30, watcher design **NOT** authored) + **Bootstrap** (C51 transfusion predicate, C52 self-bootstrap, C53 validation-gate apex). Stacked PR(s) — fence and bootstrap may split.
- **Cross-cluster integration**: one real **SEAM adversary** per product/cluster before integrate; cross-component conflicts recorded as numbered ledger decisions (D-31…) in [`review-log.md`](./review-log.md) and propagated; a ≥5-persona **opus panel** over the spine when the chunk is large.
- **Morning summary** (plain-language, idea-first, no hash-IDs in body — per [`human-scoped-deliverables`](../../../.claude/skills/human-scoped-deliverables/SKILL.md)) + **self-retrospective** at run close.

## What I plan to NOT do

- **NOT design the D-30 blocking enforcement watcher.** Binding (D-30): its design is **deferred until the empirical D-23 spike confirms Gas City does not prevent natively**. I author the fence specs to *require* prevent (native-or-watcher) and name the watcher as the sanctioned-but-undesigned discharge — I do not design it.
- **NOT run the empirical D-23 spike.** It needs a Docker-capable env (owed); it does **not** block defining components. I deliver the conformance check as a fully-runnable spec/checklist grounded in the harvest, with the live run marked owed.
- **NOT author the 32 non-spine components.** Only the 25 spine components + any **non-spine dependency a spine component strictly needs** (e.g. C24/CXDB bridge *only if* the eval tier is found to read trajectories from C21 — see decision fork 2).
- **NOT relitigate binding decisions D-1..D-30**, nor touch `archive/` or the frozen `spec-optimized/`/`plan-optimized/` reference tracks.

## Scale estimate

- **Target PR count:** ~12–16 stacked PRs (operator-directed structure: envelope+planning, Gas City core, Gas City runbook+conformance, Claude Max, Spec-intake+C20, Eval tier, Fence, Bootstrap, decision brief(s), summary+retro — split where natural).
- **Subagent count estimate:** ~70–110 (≈21 builders at Sweep-2 + ~4 runbook/conformance authors + 4 seam-refiners + 7 cross-cluster seam adversaries + per-component adversaries on load-bearing specs + 1–2 opus panels + 2-round ×≥3 reviewers per decision brief + TL;DR regen at close).
- **Expected duration:** a full overnight of live orchestration + queued subagent processing (platform caps concurrency ~6–8; pipelined throughout).

## First decision points

1. **Conformance check with no Docker-capable env.** The empirical D-23 spike (prevent-vs-detect / `[[service]]` / Orders-durability) needs Docker, which this env lacks.
   - **Lead-agent current best:** Deliver the conformance check as the **operationalized, runnable spike spec + checklist** grounded in the [D-23 harvest](./D-23-substrate-harvest.md), with the *live empirical run* explicitly marked **owed (blocked on Docker)**. Matches [`HANDOFF.md` §0.3.5](./HANDOFF.md) ("the spike is owed but does NOT block defining components").
   - **Alternative:** attempt a live `gc` build in-sandbox (Go 1.26.3) without Docker for the non-container tests. *Rewind:* the conformance-check spec is one file; revert it.
2. **Is the CXDB bridge (C24) / trajectory store (C21) a spine dependency?** The eval tier must score a trajectory; does C32 read it from CXDB (pulling C21+C24 into scope) or from the Inspect AI eval log C31 produces?
   - **Lead-agent current best:** **NO** — for the minimal safe spine, C31 produces an Inspect AI trajectory log and C32 scores **that log directly**; CXDB (C21/C22) + bridge (C24) are the *back-half* store the backbone defers. So C24 is **not** pulled in. I'll have the eval-tier cluster + cross-cluster seam adversary confirm; if they find C32 *must* read from C21, I'll define a minimal C24 and record it as a ledger decision. *Rewind:* revert the eval-tier PR + the ledger entry.
3. **C31↔C32 runner/scorer contract** (review-log C32:OQ4).
   - **Lead-agent current best:** **C31 runs → produces trajectory log; C32 scores post-hoc** off that log (Inspect AI's native scorer phase), keeping runner/scorer cleanly split and the trajectory the single hand-off artifact. *Rewind:* revert the eval-tier PR; the contract is one `ScoreRecord`/seam section.
4. **Judge read-surface SHAPE** — the owed joint **C42/C34/C32 Sweep-2 freeze** (D-17; unifies OQ-C42-3 + OQ-C34-3 + C32-OQ5).
   - **Lead-agent current best:** resolve it **in-run** as a ledger decision driven by the cross-cluster seam adversary (separate judge rig that MAY read worker trajectories + the held-out scenario partition; worker MUST NOT read the judge rig or scenarios; no shared context window). If it proves a genuine operator-judgment/security-risk-tolerance fork, I escalate it to a **two-round decision brief** and flag it as a morning-review item rather than freezing.

## What I'll surface in the morning summary (morning-review items)

- The **owed empirical D-23 spike** (needs Docker) and therefore the **still-deferred D-30 watcher design** — reaffirmed, with what the spike would decide.
- Any genuine operator-judgment fork that survives two rounds of adversarial review (candidate: the **C33 satisfaction-distribution definition**, which the inventory flags as "a non-trivial design question, not a thin wrapper"; possibly the judge read-surface shape if it turns on risk tolerance).
- Any **new cross-component ledger decision (D-31…)** that changes a Sweep-1 contract, with its rewind point.
- Any spine-dependency pulled in from outside the 25 (e.g. C24) and why.

If a fork is resolvable by adversarial review + the capability-for-principle bar, I resolve it in-run and record the rewind point rather than surfacing it.

## Stop conditions

- **Allowed stops:** context-budget approaching ~70% (write summary + retro first, then continue if possible), 30-PR cap, hard-failed dependency (GitHub/auth/subagent harness — try `github-connection-resilience` recovery first), scope-envelope completion (prefer extending into adjacent spine-dependency work over stopping), user-message-arrived interrupt.
- **Will NOT stop on:** a product/cluster closing (start the next), ambiguous subagent results (dispatch a clarifier or write a follow-up brief), a fork that feels like user-judgment (decision brief handles it).

---

## User response (implicit-confirm)

- **Confirm as-written:** implicit (operator unreachable; envelope is the contract).
- **Adjustments:** none received at run start.
- The envelope is committed as the first file of the run for rewindability; the run begins immediately after.
