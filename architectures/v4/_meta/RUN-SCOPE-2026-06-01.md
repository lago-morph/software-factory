# Scope envelope — v4 Sweep-2 run (D-23 substrate verification + implementation-ready depth)

**Author.** Lead agent, autonomous-run session 2026-06-01.
**Status.** Confirmed by operator (jonathan@manton.com) via two up-front decisions (see "Operator pre-alignment" below). Run begins.
**Branch model.** **Stacked PRs** (operator-directed, 2026-06-01). Stack bottom = `claude/architectures-v4-meta-guidance-OpJFZ` (scope envelope, PR #229, base `main`); each subsequent chunk is its own child branch `claude/architectures-v4-meta-guidance-OpJFZ-<NN>-<slug>` off the previous tip, PR base set to that parent. See decision-point #1 (resolved).
**Resume context.** [`HANDOFF.md`](./HANDOFF.md) (Sweep-2 is the next work; D-23 Gas City reality-check is its first action), [`run-summary.md`](../../../run-summary.md), [`review-log.md`](./review-log.md) (~196 harvested OQs = the Sweep-2 work list), [the expert-panel verdict](./panel/VERDICT.md) (8 NEW recommendations beyond D-20..D-25).

---

## Operator pre-alignment (answered before the run began)

1. **D-23 approach → "No live run — protocol + harvest only."** Do NOT spend tokens standing up live `claude` agents. Write the rigorous runnable D-23 spike protocol, harvest the substrate facts the `lago-morph/gascity-prototype` build already proved into the affected v4 specs, and proceed with Sweep-2 depth on components that do not depend on the unresolved prevent-vs-detect question.
2. **Run scope → "Full unattended Sweep-2 run."** D-23 milestone first, then fan out Sweep-2 implementation-ready depth across components + the 8 panel recommendations.

These two answers are the binding frame. Everything below elaborates within them.

## What I plan to do

- **D-23 milestone (protocol).** Author a runnable Gas City reality-check spike protocol ([D-23 spike protocol](./D-23-gas-city-spike-protocol.md), authored this run): exact obtain/build/run commands grounded in the prototype, plus the prevent-vs-detect, `[[service]]`-semantics, and Orders-durability test designs — so the empirical spike is a deterministic checklist for whoever has a Docker-capable sandbox/laptop.
- **D-23 milestone (harvest).** Fold the substrate facts the prototype ALREADY proved (e.g. `[[rig]]` lives in `.gc/site.toml` → XC-9; `convergence.max_iterations` is not a real field; packs load via `gc import install`; tmux-pane-per-agent controller model; bead-prefix scope `gp-`/`r1-`/`r2-`) into the affected specs as "verified-against-real-`gc`" annotations, and close the corresponding OQs in `review-log.md` / `STATUS.md`.
- **D-23 binding-consequence decision brief (`auto-001`).** Two rounds of real adversarial review on the panel's load-bearing ask (PF-1 / security / methodology): if `gc` turns out detect-not-prevent, what binding gate does that place on D-20 (fence-before-unattended) — go/no-go, compensating prevent layer, or autonomy descope?
- **Sweep-2 implementation-ready depth fan-out.** Re-enter components at Sweep-2 depth (concrete signatures, data schemas, API/message contracts, sequence/state diagrams in Mermaid, error taxonomies, concrete acceptance tests), prioritized by the products-first build order (backbone clusters first; see [`implementation-dependencies.md`](../implementation-dependencies.md)) and by OQ density, skipping/short-cutting the parts blocked on prevent-vs-detect. Subagents write to disk and return receipts; orchestrator owns all git; commit+push every wave.
- **Cheap panel fixes folded in where they touch a component being deepened** (judge_independence_tier field on C33; Unleash version-pin home; shadow-evaluator + drift-tripwire design notes), each recorded in the decision ledger.
- **Panel-of-experts review** when bringing together a large chunk of Sweep-2 output (per operator's standing instruction), with real adversarial subagents.
- **End-of-run kit:** morning summary (`run-summary.md`-style), HANDOFF refresh, AGENT-ENTRY pointer update if a phase boundary moves, TL;DR regen over tagged docs, self-retrospective.

## What I plan to NOT do

- **Stand up a live Gas City / run live `claude` agents** (operator decision #1). No image build, no order issuance, no token-burning agent fleet this run.
- **Relitigate D-20..D-25** (operator-adopted) or the panel verdict tier. The 8 panel NEW recommendations are *folded in as Sweep-2 work*, not reopened as decisions, except where one genuinely needs a brief.
- **Touch `spec-optimized/` / `plan-optimized/`** (frozen reference) or the four v4 source docs as primary-context reads (subagents read targeted sections and return receipts).
- **Finish all 57 components at Sweep-2 depth** — that exceeds one run. I deepen a prioritized chunk and hand off the long tail explicitly (see morning summary).

## Scale estimate

- **Target:** ~20–30 **stacked PRs** (operator-directed), each a reviewable chunk on its own child branch with its own rewind SHA, stacked in plan order. Do not batch into one end-of-run PR.
- **Subagents:** ~40–80 dispatches (builders + adversaries + panel), pipelined at ~6–8 concurrency.
- **Duration:** a full unattended session; bounded by the stop conditions below.

## First decision points

1. **Branch model — RESOLVED by operator (2026-06-01): use stacked PRs.**
   - The lead agent initially proposed a single pinned branch + one living PR (citing the branch-pin constraint and the Sweep-1 precedent). The **operator overrode this**: "Use stacked PRs like I told you to. I don't want one huge [PR] at the end." This is explicit permission to push stacked child branches off the pinned branch.
   - **In force:** each chunk lands as its own stacked PR (base = parent branch); the autonomous-run stacked-PR + morning-merge-order discipline applies. PR #229 is the stack bottom.
2. **Sweep-2 prioritization order.**
   - **Lead-agent current best:** D-23-affected substrate components first (C01/C12/C40/C42/C43/C34), then the safe-self-build backbone clusters (per [`implementation-dependencies.md`](../implementation-dependencies.md)), then high-OQ-density components.
   - **Alternative:** strict numeric C01→C57 order.
   - **If you disagree:** revert the relevant wave commits; ordering is not load-bearing on content.
3. **D-23 binding-consequence (auto-001) outcome.**
   - **Lead-agent current best:** make a detect-only `gc` outcome a binding go/no-go gate on D-20 (the security + methodology panelists' position), with a named compensating-prevent fallback.
   - **Alternative:** keep it a noted caveat (the as-adopted D-23 posture).
   - **If you disagree:** revert the `auto-001` commit; the brief preserves both rounds for traceability.

## What I'll surface in the morning summary (not auto-decided)

- The `auto-001` binding-consequence recommendation (genuinely a security risk-tolerance call).
- Any substrate fact from the prototype that *contradicts* a v4 "native" claim (vs. merely confirming it) — flagged loudly, since that's a spec-correctness issue.
- The explicit list of components left at Sweep-1 depth (the deferred long tail) with a suggested next-run order.
- Whether the cheap panel fixes (judge_independence_tier, shadow evaluator, drift tripwire) should lift from per-component notes to adopted decisions.

## Stop conditions

- **Allowed stops:** context-budget approaching (~70% → write summary + retro then continue if room), hard-failed dependency (GitHub/jentic/auth/subagent harness), scope-envelope completion, ~30-wave cap, user-message interrupt.
- **Will NOT stop on:** a sub-phase closing, ambiguous subagent results (re-dispatch), or a decision feeling like user-judgment territory (decision brief handles it).

---

## Operator response

- **Confirmed** via the two pre-alignment answers above (D-23 = protocol+harvest; scope = full Sweep-2). Run begins on commit of this envelope.
