# Retrospective — PLAN.md back-fill + iterative drift audit (PR #98)

- **UTC date**: 2026-05-18 (verified via `date -u +%Y-%m-%d` and `python3 -c "import datetime; print(datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%d'))"` — both agreed)
- **Last PR**: #98 (merged 2026-05-18)
- **Branch at write time**: `claude/audit-research-plan-2Zyzl`
- **Sibling artifacts**: [./2026-05-18-98/](./2026-05-18-98/)
- **Scope**: full session (from branch divergence; no prior retrospective in this session)

## Commit hashes by PR

### PR #98 — `claude/audit-research-plan-2Zyzl` (merged 2026-05-18 as `483d719`)

- `ae7e045` Install research-pipeline workflows: regen-sources-md-auto.yml, regen-sources-md-manual.yml, test-research-pipeline.yml
- `6744176` PLAN.md back-fill: 7 catalog-mutating PRs that landed without a Session bullet
- `afaf7c0` PLAN.md: iterative drift audit fixes (v0.17 cont.)

## Part 1 — what happened

The session's brief was a single sentence from the user: *"we need to audit then update research/PLAN.md. Use the research-pipeline skill for this. We are doing this because we have a number of PRs that did not update PLAN.md properly."* That framing pointed at PR-level back-fill (Session bullets missing for certain PRs), but the actual situation turned out to involve much wider drift — count drift, structural drift in the §2 repository layout, and cross-section contradictions Round-10 had introduced two weeks earlier. The session split into two phases: back-fill, then iterative audit.

### Phase 1 — pre-flight + targeted back-fill

The research-pipeline skill's SKILL.md mandates a `python install-workflows.py --check` pre-flight on every invocation. That returned exit-1 (drift between templates and installed workflows) — auto-fix by running `--force` without asking the user, per the skill's explicit instruction. The script committed its own fix (`ae7e045`), leaving me to push it later.

I then ran `python .claude/skills/research-pipeline/scripts/check-plan-consistency.py --window 50` and triaged the 20 orphan catalog commits it surfaced. Mapping each commit back to its merge via `git log --first-parent origin/main` (the `gh` CLI was not available — uses GitHub MCP tools instead), I identified 7 substantive PRs (`#71`, `#79`, `#80`, `#83`, `#84`, `#90`, `#91`) that had landed on main without a contemporaneous Session bullet in PLAN.md. The other 13 orphan commits were derived-artifact regens, merge commits, or WIP commits whose substance was covered by their parent PR.

Before back-filling, I used `AskUserQuestion` with three named options to lock in the scope ("Back-fill all 7 / drains only / plus assign round numbers"). The user picked option 1 in seconds, and I wrote a labelled **Back-fill block — PRs that landed without a contemporaneous PLAN.md bullet (audited 2026-05-18)** under §1, with one bullet per PR. Version bumped v0.16 → v0.17; Earlier-versions paragraph extended; lint passed after one missing dep install (`pip install jsonschema`). Pushed and opened PR #98 ready-for-review (per `AGENTS.md` convention, *not* draft despite the harness default suggesting otherwise).

### Phase 2 — iterative drift audit

The user followed up with *"in a loop, review the plan to make sure it is correct and there aren't any inconsistencies. If you fix it, then do another review/fix cycle until there are no major errors and no minor errors that have factual mistakes."* I invoked the `post-edit-reread-pass` skill and ran 6 iterations:

- **Iteration 1** caught the big drift. Ground-truth queries (`ls research/`, `jq 'length' reference-only/sources.json`, `ls -d .claude/skills/*/`, `ls -d retrospective/*/`) returned counts that contradicted the doc in five places: §1 status said "26 numbered reports" (actually 37 — Round-10 added 27–37); §2 layout said "8 installed skills" (actually 15) and listed `el-kaim-book/`, `chatgpt-deep-research-2026-05-11/`, `dark-factory-article.txt`, `camel-paper/`, `lenny-podcast-transcripts/` (all migrated to per-id directories by PR #80); §3.4 said "5 retrospectives" (actually 22); §17 version-history table ended at v0.13 (missing v0.14–v0.17 rows). I rewrote §2 entirely, bumped counts everywhere they appeared, added the §17 rows, and added a structural-constraint note to §3.6 (Round-10 took F40–F49, so the original triage suggestion of shifting F38/F39 → F40/F41 is no longer applicable).

- **Iteration 2** was small: §1 "Open items" pointer to §3.4 still said "now five retrospectives cumulative"; §6.2 row referred to "this v0.13 sync".

- **Iteration 3** caught a cross-section contradiction Round-10 had introduced two weeks earlier and that nobody had fixed. Round-10 Cluster F closed all three `openai.com/index/*` URLs (via manual MHTML capture) and Round-10 Cluster L closed the Princeton SWE-bench blog. But §1 status still said "~7 URLs", §3.3 fetch-queue still listed those URLs as outstanding, §4.3 table still tracked them as Path-B-only, and §10 row 5 still marked report 18 as 🟡. Catalog-verified record `f9d207b032` as `have+complete` via `jq` before fixing. Updated all five places.

- **Iteration 4** caught five "~7 URLs" residues across §4.1, §4.3 intro, §5 task 7, §9, and §14 — drift introduced by iteration 3's incomplete sweep of the §1 update. Added a §14 "Update 2026-05-16 (Round-10)" paragraph noting reports 18 + 22 both flipped 🟡 → ✅.

- **Iteration 5** added one tiny fix: §4.1's `[fetch-urls]` issue list missed #82 (added by PR #83).

- **Iteration 6** was clean: ground-truth queries matched every claim in §1 + §2; `lint-sources.sh` exited 0; `check-plan-consistency.py` reported 0 errors.

Committed the iteration fixes as `afaf7c0`. PR #98 auto-merged shortly after. The session unsubscribed automatically.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 0 |
| PRs opened / merged | 1 / 1 (#98) |
| Real-world bugs discovered + fixed | 17 distinct drift findings across 6 iterations |
| Tests added (before / after) | 0 / 0 |
| Spec amendments | 0 |
| Scenarios driven / skipped | 1 audit task / 0 |
| Files touched at major refactors | 1 (research/PLAN.md) + 3 workflow templates auto-installed |
| Iteration passes on the doc | 6 (5 substantive + 1 clean confirmation) |
| Doc lines before → after | 520 → 538 |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `status-doc-ground-truth-audit` | SKILL-SPEC-5297d70d4d | med | Audit workflow | [./2026-05-18-98/SKILL-SPEC-5297d70d4d-status-doc-ground-truth-audit.md](./2026-05-18-98/SKILL-SPEC-5297d70d4d-status-doc-ground-truth-audit.md) |

The other lessons from this session are better expressed as `AGENTS.md` rules than as new skills (see Part 3) — the existing `post-edit-reread-pass` skill already covers the iteration discipline; the existing `research-pipeline` `_plan/audit.md` resource already covers the consistency-script side. The new skill above codifies the *ground-truth-first* discipline (running filesystem / catalog / git queries *before* reading the doc), which is the load-bearing missing piece between those two skills.

## Part 3 — agents-file suggestions

Three rules. Each per-rule file in the sibling directory follows the strict two-section format (`# agent instruction` + `# justification`) for the downstream CI/CD assembler.

### Suggestion 1: Ground-truth-first when auditing status docs — `AGENTS-MD-40aa531f93`

- **Per-rule file**: [./2026-05-18-98/AGENTS-MD-40aa531f93-ground-truth-first-status-doc-audits.md](./2026-05-18-98/AGENTS-MD-40aa531f93-ground-truth-first-status-doc-audits.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Ground-truth-first when auditing status docs.** When the user asks for an audit of a long-lived status document (PLAN.md, ROADMAP.md, INDEX.md, RFC, design doc), run filesystem / catalog / git ground-truth queries (`ls research/ | wc -l`, `jq 'length' reference-only/sources.json`, `ls -d retrospective/*/`) BEFORE reading the doc's narrative claims. Counting first, reading second, fixing third catches drift that content-first reading internalizes and propagates.
>
> *Grounded in: PR #98 PLAN.md audit caught report-count drift 26→37 only after running `ls research/ | wc -l`; the prose claim had survived two prior PRs that nominally updated PLAN.md.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The dominant failure mode for long-lived status docs is that prose claims drift while the underlying counts move. PLAN.md said "26 numbered reports" in §1 status, "26 numbered reports + 12 followup" in §2 layout, and "5 retrospectives" in §3.4 — none of which matched ground truth (37 / 12 / 22). Two prior PRs (#94 plan-update-discipline, #97 single-source-normalize) both bumped the Version line but didn't recount, because they read-and-edited rather than count-and-edit; the wrong numbers carried forward each time. Cost of *not* having the rule: each subsequent audit re-discovers and re-fixes the same drift; readers learn to distrust the doc. Cost of adopting the rule: 30 seconds at the start of an audit to run 5 shell commands. The asymmetry is enormous — the rule pays for itself the first time it's followed.

### Suggestion 2: Iterate post-edit-reread-pass until clean — `AGENTS-MD-a6b01a2604`

- **Per-rule file**: [./2026-05-18-98/AGENTS-MD-a6b01a2604-iterate-post-edit-reread-until-clean.md](./2026-05-18-98/AGENTS-MD-a6b01a2604-iterate-post-edit-reread-until-clean.md)

**Proposed agent instruction** (verbatim):

> **Iterate post-edit-reread-pass until clean.** When the user asks to "iterate", "double-check", or "review yourself" on a multi-section doc edit, do not stop after iteration 1 just because iteration 1 found things — fixes from iteration N introduce drift that iteration N+1 catches. Stop only when the most recent full pass surfaces zero major or factually-wrong-minor findings.
>
> *Grounded in: PR #98 audit iterations 1–6, where iteration 3 found Round-10's openai.com/index/* URL closures that iterations 1–2 had missed, and iteration 3's own fixes introduced "~4 URLs" residues that iteration 4 cleaned up.*

**Justification** (verbatim):

The session ran 6 iterations on PLAN.md. Iteration 1 caught the big drift (count drift in §1 + §2 layout staleness + §17 missing version-history rows). Iteration 2 was small (§1 open-items pointer; §6.2 row). Iteration 3 caught a cross-section contradiction Round-10 had introduced two weeks earlier (`openai.com/index/*` URLs closed but still listed outstanding in five places). Iteration 4 caught five "~7 URLs" residues from iteration 3's own incomplete sweep. Iteration 5 added one tiny fix (#82 missing from a list). Iteration 6 was the clean confirmation pass. If we'd stopped at iteration 3 — the natural "ok this looks done" stopping point — §4.1, §4.3, §5 task 7, §9, and §14 would still carry stale "~7 URLs" claims contradicting the §1 update. Cost of adopting the rule: 5 minutes per additional pass on a 500-line doc. Cost of not adopting: the drift that survives ships, and the iteration-N+1 work that *would* have caught it never happens.

### Suggestion 3: AskUserQuestion early when audit scope is ambiguous — `AGENTS-MD-62fa132489`

- **Per-rule file**: [./2026-05-18-98/AGENTS-MD-62fa132489-askuserquestion-early-on-ambiguous-scope.md](./2026-05-18-98/AGENTS-MD-62fa132489-askuserquestion-early-on-ambiguous-scope.md)

**Proposed agent instruction** (verbatim):

> **AskUserQuestion early when audit scope is ambiguous.** When the user asks for an audit/fix task and the scope could plausibly mean "just the surface issue" or "the whole document including discovered drift", use AskUserQuestion with 2–3 named options before committing to one. The cost of a 30-second clarification round trip is far less than the cost of doing the wrong scope and re-doing it.
>
> *Grounded in: PR #98 — after identifying 7 orphan PRs needing PLAN.md back-fill, asked the user via AskUserQuestion with 3 options; user picked one in seconds and the rest of the work was unambiguous.*

**Justification** (verbatim):

The session had two ambiguity points. For the first — *which* orphan PRs to back-fill (the substantive 7, or only the drains, or all 7 with assigned round numbers) — I used AskUserQuestion with 3 named options and got a clear answer in one round trip; the back-fill work then proceeded without further scope chatter. For the second — how deep to go on the iteration sweep — the user's request ("iterate until no major or factually-wrong minor errors") was self-contained enough not to need clarification. The contrast is instructive: the explicit AskUserQuestion pattern was cheap and decisive; an open-ended "what do you want me to do here?" would have taken 2–3 round trips of clarifying chatter. Cost of adopting the rule: one AskUserQuestion call (≈ 10 seconds of agent time + ≈ 10 seconds of user click time). Cost of not adopting: starting on the wrong scope, then redoing — typically a 10× cost multiplier when the doc is long.

## Part 4 — proposed ADRs

**None this session.** The work was tactical (audit + fix one document); no binding architectural decisions were made. The closest candidates — *back-fill bullets go in a distinct labelled block, not interleaved* and *PLAN.md audits run ground-truth-first* — are practices, not architectural calls, and are captured in the `status-doc-ground-truth-audit` skill spec (Part 2) and the AGENTS rules (Part 3). The existing ADR system (see `docs/adr/`) and the existing plan-update-discipline retrospectives (e.g., `retrospective/2026-05-18-94/plan-md-is-auto-updated-by-every-catalog-mutation-ADR-377642d447.md`) already cover the higher-level architectural decisions in this area.

Reprocess note: if a future session finds that the "back-fill in a labelled block" pattern needs formalizing — e.g., the research-pipeline skill's `_plan/update-discipline.md` should specify this — that's a candidate for a small editorial-policy ADR at that point, not now.
