# spec-optimized/ — FROZEN REFERENCE (not the active track)

As of **2026-05-31** the single canonical spec track is [`../spec/`](../spec/). This `spec-optimized/`
(Track B) is **frozen reference** — do not author new work here.

It is retained for two reasons:

1. **The deferred architectural bets** — see [`../_meta/FUTURE-ENHANCEMENTS.md`](../_meta/FUTURE-ENHANCEMENTS.md)
   FE-1 (judge L2/L3), FE-2 (portability contracts), FE-3 (mandatory signing), FE-4 (multi-seat pool).
   The optimized specs are the worked-out form of those bets, to revive if/when their blockers clear.
2. **Cross-track provenance** — several canonical specs cite specific `spec-optimized/` deltas to explain
   *why* the canonical track chose what it did (e.g. C08/C09 keep the collapse instead of the standalone
   bundle). Those references point here.

The convergence rationale and the per-delta disposition (25 kept-and-already-present, 117 dropped, bets
deferred) are in [`../_meta/SURVIVOR-PASS.md`](../_meta/SURVIVOR-PASS.md).
