# plan-optimized/ — FROZEN REFERENCE (not the active track)

As of **2026-05-31** the canonical plan track is [`../plan-faithful/`](../plan-faithful/) (the build
companion to the canonical [`../spec/`](../spec/)). This `plan-optimized/` (Track B) is **frozen
reference** — do not author new work here.

It is the build-plan companion to [`../spec-optimized/`](../spec-optimized/) and is retained for the same
reasons: the deferred architectural bets (see
[`../_meta/FUTURE-ENHANCEMENTS.md`](../_meta/FUTURE-ENHANCEMENTS.md) FE-1..FE-4) and cross-track provenance.

Convergence rationale: [`../_meta/SURVIVOR-PASS.md`](../_meta/SURVIVOR-PASS.md).

> Note: `plan-faithful/` may later be renamed `plan/` for symmetry with `spec/`; that rename is an open
> follow-up, not yet done.
